# Validator Audit Report — In-App Screenshot Capture (Retroactive)

**Methodology:** Adversarial-Collaborative Audit (retroactive — commit already on `main`)
**Commit Reviewed:** `d43adbc` "Add in-app screenshot capture" (`main.cpp`, `renderer.h`)
**Author:** Codex instance in pgu-ui, pushed directly to `main` (process gap, not itself a code issue)
**Status:** **Needs Fix** — one likely-real correctness bug in the readback timing, plus a hang-risk and a minor parsing rough edge. Everything else checked out.

---

## 1. Findings

### 1.1 [High / plausible] Screenshot readback is encoded *after* `surface_.Present()`, against the same presented texture

In `renderer.h::RenderFrame()`:

```cpp
if (surface_.Present() != wgpu::Status::Success) { ... return {}; }

FrameResult result;
result.presented = true;
if (pendingScreenshotPath_.has_value()) {
    CapturePresentedSurfaceTexture(surfaceTexture.texture, *pendingScreenshotPath_);
    ...
}
```

`CapturePresentedSurfaceTexture` builds a *new* command encoder, does `CopyTextureToBuffer(surfaceTexture.texture, ...)`, and submits it — all after `Present()` already returned success for that same texture.

This is backwards relative to how WebGPU/Vulkan swapchains work. Once `Present()` succeeds, ownership of that swapchain image transfers to the presentation engine (in the Vulkan backend, that's the `vkQueuePresentKHR` handoff); the application isn't supposed to issue further GPU commands against it until it's reacquired via `GetCurrentTexture()` on a later frame. Recording a `CopyTextureToBuffer` against an already-presented swapchain texture is at best undefined (garbage/black pixels if the backend tolerates it) and at worst a validation error that Dawn's default uncaptured-error handling can turn into a hard abort — which would take down the exact automated capture path this feature exists for.

**Why this matters here specifically:** commit `5058d21` (same day) added kwin/xdotool/imagemagick tooling to the team container *specifically so `--screenshot-out`/F12 could produce real output for headless GUI testing*. If this path corrupts or crashes instead of writing a clean PNG, the thing meant to validate the app visually is itself broken.

**Fix:** encode the `CopyTextureToBuffer` into the *same* command buffer as the render pass — right after `pass.End()`, before `encoder.Finish()` — so it executes before `Present()` is ever called (WebGPU auto-tracks the render-pass-write → copy-read hazard within one encoder, no manual barrier needed). Only the buffer map + PNG write (which touches the readback buffer, not the swapchain texture) needs to stay after `Present()`.

**Not verified live:** this container has no DISPLAY/XDG_RUNTIME_DIR either (confirmed: `/tmp/pgu_screenshot_verify.log` shows `GLFW Error (65550): Failed to detect any supported platform`), so I could not reproduce an actual crash or inspect a real captured PNG. This finding is from static analysis of WebGPU/Vulkan swapchain-ownership semantics, not a reproduced failure. Recommend exercising `--screenshot-out=/tmp/x.png --screenshot-after-frames=5` for real once the new kwin headless compositor tooling is available, and diffing/inspecting the output, as the actual acceptance bar.

### 1.2 [Medium] No bound on waiting for `screenshotAfterFrames` presented frames — can spin forever

The auto-capture check in `main.cpp::MainLoop()`:

```cpp
if (options_.screenshotOutputPath.has_value() &&
    !renderer_.ScreenshotPending() &&
    presentedFrames_ + 1 >= options_.screenshotAfterFrames) {
    QueueScreenshot(*options_.screenshotOutputPath, true);
}
```

only advances `presentedFrames_` when `RenderFrame()` actually presents. If the surface never presents a frame — minimized at startup, or `GetCurrentTexture` perpetually returning `Timeout`/`Outdated` in a headless/virtual-compositor setup (this repo already has documented virtio-GPU/venus quirks around present) — the main loop just spins indefinitely waiting to reach the target count, with no error or timeout path. Given the whole point of this flag is unattended CI/script use, a silent hang here wedges the calling script instead of failing loudly with a diagnosable message.

Suggest a frame-attempt or wall-clock cap (e.g. "if N seconds/iterations pass with zero presented frames while a screenshot is outstanding, `Fail()` with a clear message") rather than spinning forever.

### 1.3 [Low / quality] `parseUint32`'s crafted error message can be bypassed by `stoull`'s own exceptions

```cpp
auto parseUint32 = [](const std::string& value, const char* flagName) {
    std::size_t consumed = 0;
    const unsigned long long parsed = std::stoull(value, &consumed, 10);
    if (consumed != value.size() || parsed == 0 || parsed > std::numeric_limits<uint32_t>::max()) {
        throw std::runtime_error(std::string("Invalid value for ") + flagName + ": " + value);
    }
    return static_cast<uint32_t>(parsed);
};
```

`std::stoull` itself throws `std::invalid_argument`/`std::out_of_range` for non-numeric or overflowing-`unsigned long long` input, before the function's own "Invalid value for ..." message can fire. Verified:

```
$ ./pgu --screenshot-out=/tmp/x.png --screenshot-after-frames=abc
stoull
$ ./pgu --screenshot-out=/tmp/x.png --screenshot-after-frames=99999999999999999999
stoull
```

Still exits 1 and is still caught by `main`'s generic `catch (const std::exception&)`, so this isn't a functional bug — just a rough edge in an otherwise carefully-messaged parser. Wrap the `stoull` call in try/catch and rethrow with the same `flagName`-qualified message for consistency.

---

## 2. What checked out

* **Row-padding/stride math:** `AlignTo(width * 4, 256)` for `bytesPerRow`, buffer sized `paddedBytesPerRow * height` — correct per the WebGPU 256-byte-alignment requirement, and the per-row de-pad loop before `stbi_write_png` (stride = unpadded width) is correct.
* **BGRA→RGBA channel remap:** correct index mapping for `BGRA8Unorm(Srgb)`; direct `memcpy` for the `RGBA8Unorm(Srgb)` case (matches the surface's own format-selection fallback in `ConfigureSurface`).
* **`CopySrc` usage flag is capability-gated:** `surfaceSupportsCopySrc_` is only set from `capabilities.usages`, and `config.usage` only OSes in `CopySrc` when supported — won't request an unsupported flag or break configuration on backends lacking it. `CapturePresentedSurfaceTexture` fails loudly (not silently) if the surface lacks `CopySrc`.
* **CLI parsing doesn't regress plain startup:** `./pgu` with no args → default `AppOptions`, no exceptions, unchanged behavior. `./pgu --help` exits 0 before any window/device creation.
* **Edge cases tested directly** (all exit 1 with a sensible message, no crash): unknown flag, `--screenshot-after-frames` without `--screenshot-out`, empty `--screenshot-out=`, `--screenshot-after-frames=0`, negative value, missing value for `--screenshot-out`.
* **No premature exit before a frame is presented:** `exitAfterPendingScreenshot_` can only lead to `glfwSetWindowShouldClose` after `frameResult.screenshotWritten`, which is only set after `result.presented = true` was already assigned earlier in the same `RenderFrame()` call.
* **Build:** incremental `cmake --build build-native` after touching both changed files compiles clean, zero warnings.
* **stb linkage:** the hand-written `extern "C" int stbi_write_png(...)` forward-declaration in `renderer.h` matches the real signature in `third_party/glfw/deps/stb_image_write.h` exactly, including `STBIWDEF` resolving to `extern "C"` under `__cplusplus` — no ODR/linkage risk.

## 3. Residual risk / confidence

Confidence in 1.1 is high but not verified by reproduction — it's the one finding I'd want re-checked with a live run (kwin headless compositor is now available per `5058d21`) before trusting this feature for automated visual validation. 1.2 and 1.3 are low-risk polish items, safe to fix opportunistically.

**Overall:** don't treat this as clean. Route 1.1 back to an implementer before leaning on `--screenshot-out`/F12 for anything that matters (e.g. Phase 1 visual validation) — the fix is small (move one copy encode above `Present()`) but the failure mode if left as-is ranges from silently wrong screenshots to a hard crash in the exact tool meant to catch rendering bugs.

---

## 4. Verification of fix (branch `fix/screenshot-present-ordering`, commit `96f3165`)

**Methodology:** re-read the full diffs line-by-line (`git diff origin/main origin/fix/screenshot-present-ordering -- renderer.h main.cpp`), rebuilt incrementally, and re-ran the same battery of CLI edge cases used in the original audit, at the same rigor.

### 4.1 [1.1 — readback-after-Present] Fixed, verified statically

`renderer.h::RenderFrame()` now, in order: `pass.End()` (line 490) → `PrepareScreenshotReadback(surfaceTexture.texture, *pendingScreenshotPath_, encoder)` which calls `encoder.CopyTextureToBuffer(...)` on the *same* encoder (lines 492–496) → `encoder.Finish()` / `queue_.Submit()` (498–499) → `surface_.Present()` (501). The copy is now recorded and submitted to the GPU strictly before `Present()` is ever called, so it reads the fully-rendered contents of the frame that is *about to be* presented, while the app still legitimately owns the texture. This closes the swapchain-ownership violation from 1.1. `WriteScreenshotPng()` (buffer map + PNG write) correctly stays after `Present()` — it only touches the readback `wgpu::Buffer`, not the swapchain texture, so that ordering is safe.

Checked the failure branch too: if `surface_.Present()` returns non-`Success` (Outdated/Lost/Timeout) on a frame where a copy was already encoded and submitted, the function returns `{}` without calling `WriteScreenshotPng` or resetting `pendingScreenshotPath_` — the already-submitted copy's readback buffer is just dropped (harmless, GPU-side, no leak since it's a refcounted `wgpu::Buffer` local), and the screenshot request stays pending to be retried on the next frame that actually presents. No mis-marking of a screenshot as "done" when it wasn't.

**Still not verified live** — same limitation as the original audit. This container has no display tooling yet: `DISPLAY`/`XDG_RUNTIME_DIR` are unset, `/tmp/.X11-unix` is empty, and `kwin_wayland`/`Xvfb`/`Xwayland` are not installed (the `5058d21` image rebuild hasn't reached this shell). Ran `./build-native/pgu --screenshot-out=/tmp/audit_verify.png --screenshot-after-frames=30` directly: it fails immediately and cleanly at GLFW init (`GLFW Error (65550): Failed to detect any supported platform`), in 4ms — confirming no regression in the no-display path, but this does **not** exercise the actual copy/readback/PNG-write code at all. That code path is still only statically verified, not reproduced. Recommend a real run once the container image is rebuilt with the kwin/xdotool/imagemagick tooling, checking the resulting PNG's dimensions/header (and ideally its content) as the true acceptance bar.

### 4.2 [1.2 — hang risk] Mostly fixed; one narrow residual gap

`FailIfAutoScreenshotStalled()` in `main.cpp` is called every loop iteration (both the minimized-continue path and the normal path), and fires `Fail()` with a clear diagnostic message (presented-frame count, target, pending status) if 10s pass with zero *newly presented* frames while a screenshot is still outstanding (`presentedFrames_ < screenshotAfterFrames || exitAfterPendingScreenshot_`). It correctly resets on every actual presentation, so it won't false-positive on a normal slow-but-eventually-presenting startup — verified by reading: `lastPresentedFrameTime_` updates in the same block that increments `presentedFrames_`, and the watchdog only compares against "time since last successful present," not overall wall-clock time. It's also correctly scoped to auto-capture only (`--screenshot-out`); interactive F12 use is exempt, as it should be.

**Residual gap:** in the minimized branch, the watchdog check runs *after* `glfwWaitEvents()`, which blocks until GLFW sees at least one event on the connection — it has no built-in timeout. In the specific "minimized at startup" scenario the fix explicitly targets, if the windowing/compositor environment produces literally zero events for longer than 10s (plausible in a degenerate headless setup with no periodic compositor activity), the watchdog's check is delayed until whatever event finally arrives, not strictly capped at 10s. This is a real but narrow edge case — it's still a large improvement over the prior unconditional hang (which had no exit at all), just not a hard 10s guarantee in that one sub-case. Not blocking, but worth a follow-up if unattended CI reliability in a truly inert compositor matters (e.g. `glfwWaitEventsTimeout()` instead of `glfwWaitEvents()`).

### 4.3 [1.3 — parseUint32 messaging] Fixed, verified directly

Rebuilt and re-ran the exact repro commands from the original finding:

```
$ ./pgu --screenshot-out=/tmp/x.png --screenshot-after-frames=abc
Invalid value for --screenshot-after-frames: abc
$ ./pgu --screenshot-out=/tmp/x.png --screenshot-after-frames=99999999999999999999
Invalid value for --screenshot-after-frames: 99999999999999999999
$ ./pgu --screenshot-out=/tmp/x.png --screenshot-after-frames=0
Invalid value for --screenshot-after-frames: 0
```

All three now report the intended flag-qualified message instead of the bare `stoull` exception text. Confirmed correct.

### 4.4 Build

Touched both changed files and ran `cmake --build build-native`: clean, zero warnings/errors.

### 4.5 Verdict

**Needs one more narrow look, not a full round.** 1.1 (the serious one) and 1.3 are fixed and hold up under the same line-by-line scrutiny as the original audit — 1.1's actual runtime behavior is still unverified for lack of a display in this container, same caveat as before. 1.2 is substantially fixed; the `glfwWaitEvents()`-has-no-timeout gap in the minimized path is a minor, non-blocking follow-up rather than a reason to reject this fix. Recommend merging, with a live `--screenshot-out` run (real PNG inspection) once display tooling is available, and optionally a follow-up swapping `glfwWaitEvents()` for a timeout-bounded wait in the minimized branch.

---

# Validator Audit Report — Phase 2 Bloom/HDR/Tonemapping Pipeline

**Methodology:** Adversarial-Collaborative Audit, static/code review (overnight autonomous work — director acting on this without waiting for the user)
**Branch reviewed:** `feature/bloom-hdr-tonemap`, commit `8a9f6dc` "Add HDR bloom and tonemapping pipeline" (`renderer.h`, `shaders/postprocess.wgsl`, `CMakeLists.txt`, `cmake/embed_shaders.cmake`), reviewed in an isolated worktree at `/home/eric/Projects/pgu-bloom-audit` (detached at `origin/feature/bloom-hdr-tonemap`, shared checkout untouched)
**Status:** **Clean on correctness, one real design mismatch found (item 4) that should be fixed before Phase 3 depends on this.** No resize/staleness bugs, no shader-math red flags, no build issues.

---

## 1. Resize correctness — verified correct

`CreatePostProcessTextures()` (allocates `hdrSceneTexture_`, `brightPassTexture_`, `blurTempTexture_`, `bloomTexture_` at the current `framebufferWidth_`/`framebufferHeight_`) is called from `ConfigureSurface()`, right after `CreateDepthTexture()` — the exact same choke point the depth buffer already relied on pre-bloom. Traced every path that can reach `ConfigureSurface()`:

* `RenderFrame()`'s `if (framebufferResized_) { surface_.Unconfigure(); ConfigureSurface(); }` — this is what the earlier deferred/settle-then-fallback drag-resize fix ultimately drives (`main.cpp`'s `ReconcileFramebufferSize`/`FramebufferSizeCallback` are unchanged by this branch — no `main.cpp` diff at all — they still just flip `framebufferResized_`, and `RenderFrame()` still reconfigures on that flag before doing anything else this frame).
* The `Outdated`/`Lost` `GetCurrentTexture` status branch — same `Unconfigure()`/`ConfigureSurface()` pair.

Both paths are checked at the very top of `RenderFrame()`, before the star pass or `RunPostProcessPasses()` touch any texture view — so a resize always fully completes (new textures + rebuilt bind groups, see §2) before that frame's postprocess passes are recorded. No stale-sized intermediate texture can get baked into a frame. Also checked the minimized case (`framebufferWidth_/Height_ <= 0`): `CreatePostProcessTextures()` early-returns exactly like `CreateDepthTexture()` already did, and `RenderFrame()` itself bails before reaching any of this — consistent, not a new gap.

## 2. Bind group staleness — verified correct

`CreatePostProcessBindGroups()` is called unconditionally at the end of **both** `CreatePostProcessTextures()` and `CreatePostProcessPipelines()`, guarded by an early-return if either the layouts or the texture views don't exist yet:

```cpp
if (singleTextureBindGroupLayout_ == nullptr || finalCompositeBindGroupLayout_ == nullptr ||
    hdrSceneView_ == nullptr || brightPassView_ == nullptr ||
    blurTempView_ == nullptr || bloomTextureView_ == nullptr) {
    return;
}
```

At startup, `InitDevice()` (calls `ConfigureSurface()` first) runs before `CreateRenderPipelines()` (calls `CreatePostProcessPipelines()` last) — confirmed via `main.cpp:276-280`. So the first `CreatePostProcessBindGroups()` call (from `ConfigureSurface`) bails early (no layouts yet), and the second (from `CreatePostProcessPipelines`) succeeds once both prerequisites exist. On every subsequent resize, only `ConfigureSurface()`/`CreatePostProcessTextures()` runs again — but the layouts already exist from startup and never change, so the guard passes and all four bind groups (`brightExtractBindGroup_`, `blurHorizontalBindGroup_`, `blurVerticalBindGroup_`, `tonemapBindGroup_`) get unconditionally rebuilt against the **new** texture views every time. No stale bind group pointing at a destroyed texture is possible — WebGPU's own ref-counting also means even the brief overlap (old bind group still referencing the old texture object at the moment the new texture is created) is safe, since a `BindGroup` holds its own reference to the resources it binds, independent of the app's own `wgpu::Texture` member being reassigned.

## 3. HDR/tonemap math — checked against standard references, no red flags

* **ACES filmic curve** (`aces_filmic` in `postprocess.wgsl`): constants `a=2.51, b=0.03, c=2.43, d=0.59, e=0.14` are exactly Krzysztof Narkowicz's well-known 2015 ACES approximation — a standard, correct reference implementation, not an ad hoc curve.
* **Bright-pass soft-knee threshold** (`fs_bright_extract`): `threshold=1.0`, `knee=0.35`. Worked through the curve by hand at several luminance values (0.9, 1.0, 1.35, 2.0) — it's monotonic, has no discontinuity at the knee boundaries, ramps in smoothly starting at `threshold - knee` and asymptotically approaches full pass-through as luminance grows, i.e. behaves exactly like the standard "soft threshold" popularized by Jorge Jiménez/Unity's bloom (this is a valid reparameterization of that same idea, not a novel/untested formula). Values aren't wildly off — a 1.0 threshold against non-tonemapped HDR scene colors and a 0.35 knee is a normal, conservative starting point.
* **Composite** (`fs_tonemap`): `bloom_strength = 0.45`, `hdr = scene + bloom * bloom_strength`, then ACES — standard add-back-then-tonemap bloom compositing, correct order (tonemap happens after combining, not before).

No live rendering was possible (see §6), so this is math/formula-level verification only, not a visual confirmation that the bloom "looks right."

## 4. Screenshot interaction — real issue: captures post-ImGui, likely wrong for Phase 3

Traced the full command-recording order in `RenderFrame()`: star pass → `RunPostProcessPasses()` (bright-extract → blur H → blur V → tonemap, writing the final tonemapped image into the swapchain `backbufferView`) → conditional ImGui pass (`LoadOp::Load` on `backbufferView`, draws the tuning panel/FPS overlay on top) → `PrepareScreenshotReadback(surfaceTexture.texture, ...)`. The screenshot copy is recorded **after** the ImGui pass, so `--screenshot-out`/F12 capture the frame **with the ImGui tuning panel and FPS counter baked in** — unchanged from the pre-bloom behavior, just now sitting on top of the tonemapped bloom output instead of the raw un-tonemapped swapchain draw.

**This is very likely wrong for Phase 3's reference-image comparison use case.** Automated pixel-diffing against reference images needs a deterministic, UI-free frame:
* The FPS counter changes every frame — guarantees a diff against any fixed reference image regardless of whether the actual rendering is correct.
* The tuning panel occupies real screen area and can visually overlap the content (stars/bloom) that Phase 3 actually wants to verify.
* Font/AA rendering of ImGui text can vary subtly across systems, adding noise unrelated to the rendering bug being tested.

Recommend: for `--screenshot-out` (the automated/CI path), skip the ImGui render pass entirely for the captured frame (or capture from a texture copy of `backbufferView` taken right after the tonemap pass, before ImGui draws) — giving Phase 3 a clean, deterministic scene-only image. F12 (interactive, manual use) can reasonably keep including the UI overlay, since a human taking a screenshot generally wants "what I'm looking at, panel included." This wasn't a regression introduced by this branch (the ordering choice predates bloom) but bloom is what makes it acutely relevant now, since Phase 3 is the reason `--screenshot-out` exists at all.

## 5. WGSL/Dawn correctness

* **Texture usage flags:** all four HDR intermediates (`hdrSceneTexture_`, `brightPassTexture_`, `blurTempTexture_`, `bloomTexture_`) get `RenderAttachment | TextureBinding` via the shared `CreateHdrTexture()` helper — exactly what each needs (rendered into by one pass, sampled by the next), nothing over- or under-provisioned. No `CopySrc`/`CopyDst`/`StorageBinding` requested anywhere they aren't needed.
* **Bind group layouts:** both `singleTextureBindGroupLayout_` and `finalCompositeBindGroupLayout_` declare `TextureSampleType::UnfilterableFloat`, matching a shader that never declares a `sampler` and only ever calls `textureLoad` — internally consistent, and this is the *correct*, in fact *required*, sample type for a texture-only (no sampler) binding of this shape. No validation mismatch.
* **On the "unfilterable textureLoad for RGBA16Float portability" rationale specifically:** the stated reasoning is shakier than the implementation choice itself. Per the WebGPU format table, `rgba16float` is filterable in core (no `float32-filterable`-style feature gate needed) — only the 32-bit float formats require that. So portability wasn't actually at risk either way. That said, going `UnfilterableFloat`+`textureLoad` **does not hurt this particular blur's quality**: the 5-tap kernel does exact discrete texel fetches at integer offsets regardless of whether the bound sample type is filterable, so every tap lands exactly where the math intends. What it *does* forgo is a possible efficiency win — the classic trick of using a linear-filtered `textureSample` to combine two adjacent taps into one interpolated fetch, roughly halving the sample count for equivalent-looking output. Given this is a full-resolution (non-downsampled), two-pass separable blur running every presented frame, that's a real perf headroom left on the table, not a correctness problem. Worth a follow-up if frame time on this path ever becomes a concern; not blocking.
* **Pass/pipeline structure:** each of the 4 fullscreen passes (bright-extract, blur×2, tonemap) is correctly `Begin`'d and `End`'d sequentially on the same encoder before the next starts (no overlapping-render-pass validation risk); none declare a depth-stencil attachment or vertex buffers, matching `vs_fullscreen`'s builtin-only, buffer-free 3-vertex trick; `colorTarget.blend` is left null (full overwrite) on all of them, consistent with every pass being a full-viewport replace.

## 6. Live verification: still not possible in this container

Checked explicitly, as instructed: `$DISPLAY`, `$WAYLAND_DISPLAY`, `$XDG_RUNTIME_DIR` are all empty; `/tmp/.X11-unix` has no socket; `/run/user/*/wayland-*` doesn't exist; `xdotool`, `wmctrl`, `kwin_wayland`, `Xwayland` are all absent from `PATH`. The `5058d21` headless-tooling image rebuild has not reached this container/worktree. Ran the built binary directly to confirm: it fails immediately and cleanly at GLFW init (`GLFW Error (65550): Failed to detect any supported platform`), same as every prior audit in this environment — no bloom-specific regression in that failure path, but this means **none of §3's shader math or §4's screenshot-ordering conclusion has been visually confirmed** — both are from reading the code and shader math by hand, not from a rendered frame. A real `--screenshot-out` run and PNG inspection (bloom glow visible and not blown out/invisible, no shear, ImGui panel present/absent as expected) remains the strongest outstanding verification step once display tooling is available.

## 7. Build

Fresh `cmake -S . -B build-native` + full `cmake --build build-native -j$(nproc)` in this isolated worktree: clean, zero warnings or errors, all targets (`glfw`, `pgu`) built successfully. `./pgu --help` still works correctly.

## 8. Verdict

**Not clean — one real, actionable finding (§4), everything else checks out.** Resize/bind-group handling (§1–2) is correctly wired end-to-end and should not regress the earlier hard-won drag-resize fix. The bloom/tonemap math (§3) is standard and sane by hand-inspection. The WGSL/Dawn texture and bind group setup (§5) is correct, with one low-priority efficiency nit (foregone bilinear-tap optimization; the stated "portability" rationale for going unfilterable doesn't hold up, but the choice itself is harmless). §4 is the one item I'd actually block on before Phase 3 builds reference-image comparisons on top of `--screenshot-out`: capturing post-ImGui will poison any pixel-diff against a clean reference image. Recommend: merge the bloom pipeline itself, but route a small follow-up to make `--screenshot-out` capture pre-ImGui (or suppress ImGui for that specific captured frame) before Phase 3 depends on it. Live visual confirmation of the bloom output and the screenshot ordering is still outstanding, blocked on display tooling not yet being present in this container.

---

# Validator Audit Report — Screenshot-Pre-ImGui Fix (Lightweight)

**Methodology:** Quick confirmation pass (director pre-diffed and pre-built; scope explicitly small/mechanical)
**Branch reviewed:** `fix/screenshot-pre-imgui`, commit `fcc285e` "Capture screenshots before ImGui overlay" (`renderer.h` only), reviewed in an isolated worktree at `/home/eric/Projects/pgu-ssfix-audit` (detached at `origin/fix/screenshot-pre-imgui`, shared checkout untouched), addressing §4 of the bloom-pipeline audit above.
**Status:** **Clean.**

**Diff scope confirmed:** `git show fcc285e` touches only `renderer.h`, 6 lines added / 6 removed — a pure relocation of the existing `screenshotReadback`/`PrepareScreenshotReadback` block, no logic changes, no other files touched. Matches the director's description exactly.

**1. Ordering, verified by reading the merged `RenderFrame()`:** star pass → `RunPostProcessPasses(encoder, backbufferView)` (writes the final tonemapped scene into the swapchain texture) → `PrepareScreenshotReadback(surfaceTexture.texture, ...)` (now here, encoding `CopyTextureToBuffer` into the same encoder) → ImGui scissor-guard + conditional ImGui pass (`LoadOp::Load`, draws on top) → `encoder.Finish()` / `queue_.Submit()` → `surface_.Present()`. This satisfies both properties at once: the copy is still encoded and submitted strictly before `Present()` (the earlier swapchain-ownership fix stays intact — nothing moved relative to `Present()`, only relative to the ImGui pass), and it now runs after the tonemap write but before the ImGui draw, so it captures the correct texture: final tonemapped scene, clean of UI overlay. Also confirmed the copy command sits validly between two render passes (tonemap pass already `End()`ed, ImGui pass not yet begun) — no overlapping-render-pass hazard from interleaving a copy there.

**2. No broken dependency on ImGui-relative ordering:** the moved block only populates the local `std::optional<ScreenshotReadback> screenshotReadback` — no other side effects. The consuming code (`if (screenshotReadback.has_value()) { WriteScreenshotPng(...); pendingScreenshotPath_.reset(); result.screenshotWritten = true; }`) is unchanged and unmoved, still runs after `Present()` succeeds, and only branches on *whether* a copy was queued this frame, not *when* in the encoder it was recorded — so it's agnostic to the reorder. `exitAfterPendingScreenshot_` (in `main.cpp`, untouched by this diff) keys off `result.screenshotWritten` exactly as before. No logic depends on capture-relative-to-ImGui ordering that could have broken.

**3. Build:** fresh `cmake -S . -B build-native` + full `cmake --build build-native -j$(nproc)` in this isolated worktree — clean, zero warnings/errors.

**Not verified live** — same standing caveat as every prior audit in this environment: `DISPLAY`/`WAYLAND_DISPLAY`/`XDG_RUNTIME_DIR` are all empty and `xdotool`/`kwin_wayland` aren't installed here. Static/code review only; a real `--screenshot-out` run (confirming the resulting PNG has no ImGui panel/FPS text baked in) is still the outstanding acceptance step once display tooling exists.

**Verdict: clean, no further changes needed.** This closes bloom-audit finding §4.

---

# Validator Audit Report — Dust Extinction (Phase 2, deep-dive)

**Methodology:** Adversarial-Collaborative Audit, full depth (same rigor as the bloom-pipeline audit), static/code review — overnight autonomous work, director acting on this without waiting for the user
**Branch reviewed:** `feature/dust-extinction`, commit `5162ab2` "Add dust extinction rendering" (`galaxy_model.h`, `galaxy_system.h`, `gpu_types.h`, `main.cpp`, `renderer.h`, `shaders/dust_extinction.wgsl`, `tuning_panel.h`, `CMakeLists.txt`, `cmake/embed_shaders.cmake`; 604 insertions), reviewed in an isolated worktree at `/home/eric/Projects/pgu-dust-audit` (detached at `origin/feature/dust-extinction`, shared checkout untouched)
**Status:** **Clean on everything I could check statically.** No bugs found in any of the 9 focus areas. One genuinely interesting non-bug finding (sorting is correctly implemented but not actually load-bearing given the blend mode — see §2) and one design-scoping judgment call the director asked for directly (§4). This branch has more riding on an actual visual check than any prior audit — see §9.

## 1. LOD tier genuinely separate from the star hierarchy — confirmed independent

Traced every piece of the dust path end-to-end in `galaxy_system.h` and it is a fully parallel structure, not stars-with-dust-mixed-in:

* **Separate leaf generation:** `GenerateDustSamples()` draws a fixed `kDustLeafCount = 24000` dust samples via its own rejection-sampling loop over disk components — reuses `SpiralArmPitchTheta`/`SpiralArmDensityFactor` (the extracted arm-density helpers, see §8) for spatial placement, but with a **deliberately different, documented arm-bias curve**: dust's acceptance probability is `normalized²` vs. stars' plain `normalized` (comment: "dust is more tightly arm-biased than stars") — i.e. this is evidence the implementer actually thought about dust's different density characteristics rather than blindly copying star logic.
* **Separate tree:** `dustHierarchyNodes_`, `dustRootNodeIndex_`, `dustNodeExpandedLastFrame_` are all distinct members from `hierarchyNodes_`/`rootNodeIndex_`/`nodeExpandedLastFrame_`. `BuildDustHierarchyFromSamples()` is its own Morton-sort + k-way merge function, textually separate from `BuildHierarchy()` (the star tree). Both happen to use a 4-way merge fanout (`kMergeFanout = 4`, each declared as an independent local constant in its own function) — that's a shared generic tree-shape parameter, not reused density/LOD tuning.
* **Independently tuned LOD threshold:** `kDustPixelErrorThreshold = 0.14f` (fixed) vs. stars' `pixelErrorThreshold_ = 0.08f` (UI-adjustable) — dust is deliberately coarser, a real, distinct tuning choice, not a shared value. (`kLodHysteresisBand = 0.25f` is shared between `SelectCut`/`SelectDustCut`, but that's a generic anti-flicker band fraction, not a density characteristic.)
* **Separate GPU cap:** `kMaxDustGaussians = 4096` (`gpu_types.h`) vs. `kMaxCutGaussians = 16384` — distinct buffers, distinct cut-count members (`dustGaussianCount_` vs. `cutGaussianCount_`).
* **Separate emission path:** `EmitDustGaussian()` is its own function. It reuses the same EWA-splat screen-space projection math as `EmitGaussian()` — appropriate, since that math (project a 3D Gaussian covariance to a 2D screen ellipse) is generic to *any* Gaussian splat, star or dust cloud; duplicating it isn't a sign of "stars-with-dust-stuffed-in," it's the correct amount of reuse for genuinely shared geometry math.

One minor observation, not a bug: the dust tree repurposes the existing `GaussianNode.brightness` field to hold *accumulated optical depth* (summed across merged children) rather than energy-conserving luminous brightness. Summing optical depth across merged dust clumps is a reasonable coarse-LOD proxy (thin-medium extinction is roughly additive along similar sightlines) but it's a different accumulation semantic riding on a field literally named `brightness` — a one-line comment at the field's dust usage site would save a future reader a double-take.

## 2. Sorting: correctly implemented, but not actually load-bearing for this blend mode

`SortDustCutBackToFront()` (`galaxy_system.h`) sorts descending by `dustSortDepths_[i] = Dot(camForward_, rel)` — a real per-splat camera-forward distance, and descending order is genuinely back-to-front (farthest first, nearest last). It's called inside `BuildCut()` immediately after `SelectDustCut()` and before the caller ever reads `DustPositions()/DustStatic()` for upload (`main.cpp`: `BuildCut()` → `UploadDustCut()` → `RenderFrame()`, every frame, no caching) — so the ordering-and-freshness half of this focus area is clean: real depth, right point in the frame, recomputed every frame, never stale.

**But here's the finding:** I traced the actual blend state (`renderer.h`): color blend is `srcFactor=Zero, dstFactor=Src, op=Add` → `result = dst_old * src_color`. Scalar multiplication is commutative and associative (up to negligible floating-point rounding) — so for *any* two dust splats overlapping the same pixel, `dst * src_A * src_B == dst * src_B * src_A` regardless of draw order. **The final image is provably identical regardless of what order the dust splats are drawn in, under this exact blend equation.** The back-to-front sort is correctly implemented and would matter under an order-dependent blend (e.g. standard alpha-over), but under *this* multiplicative blend it's not actually doing anything for correctness today. Not a bug — it doesn't produce a wrong image, and it's cheap (see below) — but worth knowing, since a future reader could reasonably (and incorrectly) assume the sort is load-bearing and be reluctant to touch it, or conversely assume the blend mode must be order-dependent because a sort exists. Worth a one-line comment noting the sort is currently precautionary/future-proofing rather than required, or removing it if the blend model is expected to stay multiplicative.

**Perf:** `n <= 4096`, `std::sort` with a trivial float comparator — tens of microseconds at most, utterly negligible next to a full-resolution multi-pass GPU pipeline (bloom alone is 4 full-screen passes/frame per the earlier audit). Not a frame-time concern. One small, unrelated nit: `SortDustCutBackToFront()` heap-allocates three new `std::vector`s (`sortedPositions`/`sortedStatic`/`sortedDepths`, up to 4096 elements each) *every frame*, where the surrounding code already has a pattern of pre-sizing persistent member scratch space once (`dustSortDepths_.resize(kMaxDustGaussians)` in the constructor) — reusing pre-sized scratch members instead of fresh per-frame vectors would match that pattern and avoid per-frame heap churn. Low priority, not a correctness issue.

## 3. Blend mode correctness — verified, attenuates bloom's source correctly, no double-counting

Confirmed the exact `wgpu::BlendState` in `renderer.h`: `dustBlend.color = {srcFactor: Zero, dstFactor: Src, op: Add}` (→ `dst' = dst * src`, real multiplicative attenuation — "light passing through dust gets dimmer" is exactly this operation) and `dustBlend.alpha = {srcFactor: Zero, dstFactor: One, op: Add}` (→ alpha passes through unmodified — deliberate, harmless, since none of the downstream postprocess shaders read alpha).

Traced the pass ordering in `RenderFrame()`: star pass (Clear, writes `hdrSceneView_`) → dust pass (`LoadOp::Load` on the *same* `hdrSceneView_`, multiplies in place) → `RunPostProcessPasses()` (bright-extract reads `hdrSceneView_`, ..., tonemap reads `hdrSceneView_` again as the "scene" term). Because the dust pass modifies `hdrSceneView_` in place, **exactly once**, before either downstream consumer reads it, both the bright-pass/bloom contribution *and* the direct "scene" term in the final composite inherit the dust attenuation exactly once each — not zero times (dust does affect bloom), not twice (no double-attenuation). This is a clean, correct design: attenuate the one shared buffer before anything branches off it, and every branch downstream automatically sees the same already-attenuated data.

## 4. Scoped-out star/dust depth interleaving — my honest read: acceptable for now, but flag it, don't silently accept it

The implementation attenuates the *entire accumulated* HDR scene through one multiplicative dust pass, rather than compositing dust against individual stars by depth. Concretely, this means: a star that is genuinely *nearer* to the camera than a dust lane will still be dimmed by that dust lane, when physically it shouldn't be (its light never passes through dust on the way to the camera). That is a real, physically-wrong case, not a hypothetical one.

**Why I think it's an acceptable simplification for this phase:** this is a galaxy-scale board renderer, not a per-star close-up renderer (per project context, the galaxy/gaussian view is the game's board). At galaxy-wide viewing distances, real interstellar dust lanes in actual spiral galaxies behave visually almost exactly like this approximation — a comparatively thin foreground screen relative to the enormous depth of the stellar disk behind it, producing the well-known "dark lane across a swath of the image" look in real astrophotography. At that scale, whole-buffer attenuation and true per-star depth compositing would likely be visually indistinguishable.

**Where it stops being acceptable:** the moment the camera gets close to or inside the disk plane, or does an oblique pass across a single arm — situations where a viewer could plausibly see a star clearly nearer than a dust lane and still watch it get dimmed. That's the kind of thing a human notices immediately and reads as "wrong," not "stylized."

**My recommendation:** don't block merging this on it — it's a reasonable, clearly-flagged, bounded choice for Phase 2 — but do explicitly flag it as a known structural limitation for Phase 3, not just a performance corner-cut. Concretely: if Phase 3's reference images are ever generated from a close/oblique camera angle, or from any renderer/tool that does true depth compositing, there will be a *systematic* mismatch near dust lanes that no pixel-diff tolerance will paper over — that's a "the reference methodology needs to account for this," not "the renderer has a bug" situation. Worth a one-line note in whatever Phase 3 planning doc governs reference-image generation, so nobody spends time chasing a "diff" that's actually this known, intentional approximation.

## 5. fBM/value-noise correctness — standard, correct structure

`fbm()` in `dust_extinction.wgsl`: 5 octaves, amplitude halves each octave (`amplitude *= 0.5`, persistence 0.5 — standard default) while frequency roughly doubles (`p * 2.03 + offset` — the `2.03` instead of exactly `2.0` is a common, deliberate trick to avoid octave-to-octave grid alignment artifacts), normalized by the running sum of amplitudes so the output stays in a consistent range regardless of octave count (important since the caller feeds this into a `smoothstep(0.40, 0.88, ...)` threshold). `value_noise()` is standard hash-based bilinear value noise with the classic smootherstep interpolant (`f*f*(3-2f)`). No irregularities — this is a textbook-correct fBM, not a novel/untested formula.

## 6. Resize correctness — no new risk, and mostly not applicable

The dust position/static vertex buffers (`dustPositionBuffer_`, `dustStaticBuffer_`) are fixed-capacity (`kMaxDustGaussians = 4096`), created once in `CreateGeometryBuffers()` (called once at startup from `main.cpp`, alongside `InitDevice()`/`CreateRenderPipelines()` — confirmed, not called from `ConfigureSurface()` or anywhere in the resize path). They are **not sized from `framebufferWidth_`/`framebufferHeight_`** at all — same as the pre-existing star cut buffers always were — so there's no analogous "did resize recreate this at the new size" question to ask; there's no size dependency to begin with.

The one screen-size-dependent resource the dust pass touches is `hdrSceneView_`, which it doesn't own or create — it just draws into the *same* HDR texture the star pass and bloom pipeline already use, and that texture's resize handling was already verified correct in the bloom audit (§1–2 there) and is untouched by this branch. No new resize-staleness risk introduced.

## 7. Per-channel wavelength extinction — correct direction

`DustSample::extinction = {0.42, 0.66, 1.00}` (R, G, B — confirmed this codebase's established channel order from the bloom audit's luminance-weight check) combined with `transmittance = exp(-extinction * optical_depth)` in the fragment shader: since `exp(-x)` decreases as `x` grows, the **largest** extinction coefficient gets the **most** attenuation. Blue (1.00) is attenuated most, red (0.42) least, green (0.66) in between — correctly directional interstellar reddening (shorter wavelengths scattered/absorbed more), matching real extinction curves in direction even though it isn't a photometrically exact CCM/Fitzpatrick curve (which is fine — the ask was directional correctness, not photometric accuracy).

## 8. Scope check: `galaxy_model.h` changes are genuinely just arm-density reuse

Diffed `galaxy_model.h` in full: it's a pure extraction refactor. The exact same `innerRadius`/`spiralB`/`bandWidth`/`band`/`thetaPitch` formula that used to be inline in `GenerateStar()` is now a named function `SpiralArmPitchTheta()`; the exact same `1 + armContrast*cos(armCount*(angle-thetaPitch))` formula is now `SpiralArmDensityFactor()`. Byte-for-byte identical math, just made callable so the dust generator can reuse it. Cross-checked against `docs/stellar_population_framework.md` (taxonomy/color/luminosity population sub-tables) — nothing in this diff touches color, spectral type, luminosity, or population tables at all; it's purely geometric arm-density placement math, orthogonal territory. Scoping claim confirmed, no stealth stellar-population work.

## 9. Live verification: not possible, and this is the audit where that caveat matters most

Checked explicitly, as always: `DISPLAY`, `WAYLAND_DISPLAY`, `XDG_RUNTIME_DIR` all empty, no X11/Wayland socket, `xdotool`/`kwin_wayland`/`Xwayland` not installed in this container. Fresh `cmake -S . -B build-native` + full `cmake --build build-native -j$(nproc)`: clean, zero warnings, all targets built; `./pgu --help` works.

**I want to be plain rather than hand-wave confidence here, per the director's ask:** this feature has more riding on an actual rendered image than any prior audit in this repo. Everything above is verified by reading code and shader math by hand — I have **not** visually confirmed: whether the fBM filament pattern actually looks like plausible dust lanes at real scale, whether the `0.035–0.155` optical-depth range times `kDustOpticalDepthScale=0.18` produces a visually subtle-vs-invisible-vs-overwhelming effect, whether the dust splats' Gaussian footprints (`effRadius` from `3σ` of the covariance) look reasonably sized against the arm width at typical camera distances, or whether the whole-buffer attenuation from §4 looks acceptable or visually jarring in practice. All four of those are the kind of thing that can look completely different in a real image than the math suggests on paper. A real `--screenshot-out` run and visual inspection — ideally at a few different camera distances/angles, specifically including one close/oblique pass near a dust lane to sanity-check §4's judgment call — is the strongest, and still entirely outstanding, verification step here.

## 10. Verdict

**Clean — no correctness bugs found in any of the 9 focus areas**, across LOD-tree independence, sort/blend correctness, fBM structure, resize handling, wavelength direction, and scope discipline. Two things worth carrying forward, neither blocking: (a) the back-to-front sort is correct but not currently necessary given the multiplicative blend — cheap enough either way, just worth a comment so nobody's confused about which is true; (b) the whole-buffer depth-interleaving simplification (§4) is a reasonable Phase 2 choice that should be explicitly flagged for Phase 3's reference-image methodology, not silently carried forward as if it were physically exact. The build is clean. The one thing I can't respond to with real confidence is what this actually looks like — that's squarely a "get a real screenshot" item, not a code-review item, and I'd treat this branch as provisionally-approved-pending-a-look rather than fully clean until someone (human or a display-capable agent) actually sees it render.

---

# Validator Audit Report — Emission Nebulae (Phase 2, deep-dive, last item)

**Methodology:** Adversarial-Collaborative Audit, full depth (same rigor as the dust-extinction audit), static/code review — overnight autonomous work, director acting on this without waiting for the user
**Branch reviewed:** `feature/emission-nebulae`, commit `05a0d88` "Add emission nebulae rendering" (`galaxy_system.h`, `gpu_types.h`, `main.cpp`, `renderer.h`, `tuning_panel.h`; 365 insertions; **zero changes to `galaxy_model.h`** and no new shader file — confirmed, see §7), reviewed in an isolated worktree at `/home/eric/Projects/pgu-nebulae-audit` (detached at `origin/feature/emission-nebulae`, shared checkout untouched)
**Status:** **Clean.** No correctness bugs across any of the 8 focus areas. Same standing caveat as the dust audit: this needs a real screenshot before being called fully done.

## 1. LOD tier genuinely separate from both star and dust hierarchies — confirmed independent

`nebulaHierarchyNodes_`/`nebulaRootNodeIndex_`/`nebulaNodeExpandedLastFrame_` are distinct members from both `hierarchyNodes_` (star) and `dustHierarchyNodes_` (dust). `BuildNebulaHierarchy()`/`BuildNebulaHierarchyFromSamples()` is its own Morton-sort + k-way-merge function (4-way fanout, same generic tree shape as star/dust but its own local constant, not a shared/reused merge routine). Sanity-checked the implementer's node-count claim by hand: a 4-ary merge tree over 1800 leaves produces levels of size 1800 → 450 → 113 → 29 → 8 → 2 → 1, summing to exactly **2403** nodes — matches their CPU probe exactly, which is good independent evidence this is a real tree being built, not a fabricated number. Own threshold (`kNebulaPixelErrorThreshold = 0.028`, notably *finer* than dust's 0.14 and even stars' 0.08 — nebula expands to leaf-level detail far more readily, consistent with wanting individually-distinct knots rather than dust's soft merged lanes), own cap (`kMaxNebulaGaussians = 2048`, `gpu_types.h`), own buffers, own emit function (`EmitNebulaGaussian`, reusing the same generic EWA splat-projection math as star/dust emission — appropriate shared geometry, not tree aliasing). This is a genuine third independent tier.

## 2. Placement/arm-density reuse and cubed-bias sanity — confirmed deliberate, not arbitrary

`GenerateNebulaSamples()` calls `SpiralArmPitchTheta(...)` and `SpiralArmDensityFactor(...)` directly (`galaxy_system.h:184,192`) — the exact same shared helpers dust uses, not reimplemented. Combined with the confirmed zero-diff on `galaxy_model.h` (§7), this is real reuse, not incidental similarity.

The acceptance-bias progression across all three tiers is now: stars use `normalizedArm` linearly, dust uses `normalizedArm²` (per the dust audit), nebula uses `normalizedArm³` here. That's a **monotonically increasing concentration toward arm peaks** as tracer type gets more tightly arm-correlated — and that ordering matches real astrophysics: HII regions/emission nebulae are the tightest, most classic visual tracer of spiral structure (young massive stars die close to where they're born), more concentrated than the general dust distribution, which is itself more concentrated than the full (dynamically-relaxed, age-mixed) stellar population. This reads as a coherent, reasoned design across three separate implementer sessions, not a coincidence or an arbitrary pick.

**"Individually distinct crisp knots" plausibility:** 1800 total leaves against a 2048 cap means the cap is *not* the binding constraint here (unlike dust, where 24K leaves >> a 4096 cap forces heavy reliance on merged/coarse nodes) — nebula can render close to every individual leaf as its own splat without cap-driven forced merging; LOD merging only kicks in when a node's projected size actually gets small with distance. 1800 discrete star-forming knots across a spiral galaxy's arms is also a plausible order-of-magnitude count for HII-region-scale features (real spirals like the Milky Way have on the order of hundreds to low-thousands of cataloged significant HII regions). This supports the "individually distinct crisp knots" goal — not too sparse (1800 is a lot of features across several arms) and not effectively continuous (no cap-forced merging into a haze at typical viewing distances).

## 3. No-sorting reasoning — verified correct

`nebulaAdditivePipeline_ = createSplatPipeline(&additiveBlend);` reuses the *exact same* `additiveBlend` `wgpu::BlendState` variable already used for `splatAdditivePipeline_` (stars) — not a separately-constructed lookalike, the literal same object. Confirmed its definition: `srcFactor=One, dstFactor=One, operation=Add` on both color and alpha components → `dst' = dst + src`. Addition is commutative and associative, so accumulating any number of nebula splats over a pixel gives the same result regardless of draw order — exactly the same order-independence property stars have always relied on (stars have never been sorted either; this isn't a new argument, it's the established pattern extended consistently). No sort call exists anywhere in the nebula path (`SelectNebulaCut` doesn't invoke one, and `BuildCut()` doesn't call one for nebula) — matches the no-sorting claim, and the reasoning holds up under direct verification of the actual blend factors, not just by analogy.

## 4. Pass ordering, HDR feed, and the dust-attenuation question — verified correct ordering; honest read on the physics below

**Ordering, confirmed from `renderer.h`:** star pass → dust pass (multiplicative, `LoadOp::Load` on `hdrSceneView_`) → nebula pass (additive, also `LoadOp::Load` on the same `hdrSceneView_`) → `RunPostProcessPasses()` (bright-extract/blur/tonemap). Nebula's contribution is added to `hdrSceneView_` strictly *after* dust's multiplicative pass has already run and strictly *before* the single tonemap pass reads the buffer — so nebula correctly feeds the bloom bright-pass and gets tonemapped exactly once, never missed, never double-tonemapped. Mechanically exactly as claimed.

**The physics, since dust's multiply already ran before nebula adds its light, nebula's own glow is *not* attenuated by dust at all** — it sits fully at its own brightness on top of whatever's left after dust dimmed the stars. The director's question is genuinely good: HII regions really are physically embedded in the same dusty molecular clouds that formed them, so in reality their light absolutely does get extincted by surrounding dust — so is the current "nebula bypasses dust" choice wrong?

**My honest read: it's a defensible, not obviously wrong, choice at this level of approximation — I wouldn't call the alternative "more correct" either.** The reason: the *existing* dust model (already accepted in the prior audit) is a whole-screen-buffer attenuation with no depth/spatial awareness — it doesn't know which specific stars are behind a given dust concentration vs. in front of it. If nebula were also run through that same non-depth-aware multiply, you'd get a *second* application of an already-approximate effect, and the actual visual risk cuts the *other* direction from what you'd naively expect: since dust and nebula are both concentrated in the same disk/arm regions (both use arm-density placement), a nebula knot sitting in the same on-screen area as a dust concentration would get dimmed by dust *regardless of whether that specific gas is actually behind that specific dust in 3D* — quite possibly muddying or washing out the very knots that are supposed to read as bright, crisp features (the doc's explicit goal from §2). Given neither path does true depth compositing, "nebula bypasses dust" is at least as reasonable as "nebula gets multiplied by dust too," and arguably safer for the stated visual goal (crisp, bright, individually-distinct knots) than compounding two independent whole-buffer approximations.

**Recommendation:** treat this the same way as the dust audit's §4 — not a bug, but flag it explicitly as a known, deliberate simplification for Phase 3's methodology (a renderer or reference set that assumes physically-correct dust-occludes-nebula compositing will show a systematic difference here, especially in dense knot-in-dust-lane regions), rather than silently treating it as settled physics. I'd revisit this specific choice only if/when true depth-aware compositing gets built for dust generally — at that point nebula should clearly go through it too, for the reason the director gave.

## 5. Color sanity — reads as pink-red, not orange or over-purple, matches the "natural H-alpha, not Hubble-palette" intent

`color = ClampColor({1.0, 0.26 + 0.12·blueLift, 0.42 + 0.24·blueLift})` for `blueLift ∈ [0,1]`, i.e. `R=1.0` always, `G∈[0.26,0.38]`, `B∈[0.42,0.66]`. Since `B` is consistently higher than `G` (0.42 > 0.26 at minimum, 0.66 > 0.38 at maximum) but never reaches or exceeds `R`, this sits on the pink/rose/magenta-leaning side of red rather than the orange side (orange would need `G > B`) — at `blueLift=0` it's a deeper rose-red, at `blueLift=1` a lighter pink with more blue-white OB-star lift, and it never approaches true magenta/purple since `B` stays well below `R` throughout. That's a good match for "natural-color H-alpha pink-red" as actually seen in real broadband (non-Hubble-palette) nebula astrophotography, and is clearly distinct from a Hubble-palette look (which would involve strong green/teal presence from mapped `[OIII]`/`[SII]`, absent here entirely). `ClampColor` itself is a pre-existing helper from the untouched `galaxy_model.h`, already used for star color tinting — reused, not reinvented.

## 6. Resize correctness — no new risk, same conclusion as dust

`nebulaPositionBuffer_`/`nebulaStaticBuffer_` are fixed-capacity (`kMaxNebulaGaussians = 2048`), created once in `CreateGeometryBuffers()` (confirmed same startup-only call site as the dust buffers, called once from `main.cpp` alongside `InitDevice()`/`CreateRenderPipelines()`, never from `ConfigureSurface()` or anywhere in the resize path). Not sized from `framebufferWidth_`/`framebufferHeight_` at all. The only screen-size-dependent resource the nebula pass touches is `hdrSceneView_`, which it doesn't own — same already-verified resize handling from the bloom audit applies, untouched by this branch. No new resize-staleness risk.

## 7. Scope check — confirmed, zero `galaxy_model.h` changes, no scope creep

`git diff origin/main HEAD -- galaxy_model.h` returns **zero lines** — independently reconfirmed the director's own check. Full file list touched by this branch: `galaxy_system.h`, `gpu_types.h`, `main.cpp`, `renderer.h`, `tuning_panel.h` — no doc files, no `stellar_population_framework.md`, nothing touching color/taxonomy/population tables. Scope fully respected.

## 8. Live verification: not possible, same caveat as dust, said plainly

Checked explicitly: `DISPLAY`/`WAYLAND_DISPLAY`/`XDG_RUNTIME_DIR` all empty, no X11/Wayland socket, no `xdotool`/`kwin_wayland`/`Xwayland` in this container. Fresh `cmake -S . -B build-native` + full `cmake --build build-native -j$(nproc)`: clean, zero warnings, all targets built; `./pgu --help` works.

Like dust, this feature has real open questions that only a rendered image can answer: does 1800 knots at the cubed arm-bias actually look like "many individually distinct knots" rather than a handful of blobs or a diffuse haze, does the pink-red color read correctly once alpha-blended additively against the star field and then bloomed/tonemapped (HDR bloom can shift perceived color/saturation in ways hand-tracing the math doesn't capture), and — most interesting given §4 — does "nebula un-attenuated by dust" look fine or does it visibly look like nebula glow floating in front of dust lanes it should arguably be dimmed by. None of that is answerable from code alone. Same recommendation as dust: get a real `--screenshot-out` run and look at it, ideally including a view where a nebula knot and a dust lane visually overlap on screen, to sanity-check §4's judgment call concretely.

## 9. Verdict

**Clean — no correctness bugs found across any of the 8 focus areas.** LOD-tier independence, arm-density reuse, blend/no-sort reasoning, pass ordering, color, resize handling, and scope discipline all check out under direct verification (not just plausibility — node counts were hand-computed and matched, blend states were confirmed to be the literal same object as the already-trusted star pipeline, `galaxy_model.h`'s diff was independently confirmed empty). The one substantive judgment call — whether nebula should be attenuated by dust — has a real, non-obvious answer: I don't think either choice is clearly "more correct" given neither dust nor nebula do true depth compositing, and the implemented choice (nebula bypasses dust) is arguably the safer one for the stated "crisp distinct knots" visual goal. Flag it for Phase 3's methodology, don't block on it. Build is clean. Same standing caveat as dust: this needs a real screenshot before it's fully done, and I'd treat it as provisionally-clean-pending-a-look, consistent with how the dust audit was left. With this being the last Phase 2 item per the director, the natural next step is the full bloom+dust+nebulae integration build/sanity-check on `main` the director mentioned, followed by an actual visual pass across all three features together — that combined view is likely to surface anything the math-level review here can't.

---

# Validator Audit Report — Renderer Code-Health Split (behavioral-equivalence check)

**Methodology:** Adversarial-Collaborative Audit, same bar as the prior App-decomposition audit (`fcfb87e`) — line-by-line comparison of moved code against its pre-refactor original, not just plausibility review. Overnight autonomous work, director acting on this without waiting for the user.
**Branch reviewed:** `refactor/renderer-decomposition`, commit `f07f5d7` "Split renderer maintenance helpers" (`renderer.h` 1372→1032 lines, plus two new files: `renderer_postprocess.h` 244 lines, `renderer_screenshot.h` 165 lines), reviewed in an isolated worktree at `/home/eric/Projects/pgu-split-audit` (detached at `origin/refactor/renderer-decomposition`, shared checkout untouched). Base commit is `64fc270`, the fully-merged bloom+dust+nebulae state.
**Status:** **Clean.** Verified as genuine code motion, not a rewrite — this doesn't need a display environment, and doesn't get one anyway (checked, none available).

## 1. Diff verified as genuine code motion, not rewritten-with-subtle-changes

Extracted the pre-refactor `renderer.h` (`git show 64fc270:renderer.h`, 1372 lines) and compared every moved function against its new location, not by trusting the diff summary but by reading both sides in full:

* **`renderer_postprocess.h`:** `CreatePostProcessPipelines`→`CreatePipelines`, `CreatePostProcessTextures`→`CreateTextures`, `CreateHdrTexture`, `CreatePostProcessBindGroups`→`CreateBindGroups`, `CreateSingleTextureBindGroup`, `RunPostProcessPasses`→`RunPasses`, `DrawFullscreenPass`, and `Shutdown`'s bloom-resource teardown block — every one of these is **byte-for-byte identical body logic** to the original. The only differences are the necessary and expected adaptations for crossing a class boundary: `CreatePipelines`/`CreateTextures` now take `device`/`surfaceFormat`/`framebufferWidth`/`framebufferHeight` as explicit parameters (assigned to the class's own copies) instead of reading `Renderer`'s shared members directly, `CreateFullscreenPipeline`/`CreateHdrTexture`/`CreateSingleTextureBindGroup` gained a `const` qualifier (correct — none of them mutate state, confirmed by re-reading each), and `DrawFullscreenPass` became `static` (correct — it only touches its own parameters, never `this`).
  One nuance worth recording: `CreateTextures`'s `if (framebufferWidth <= 0 || framebufferHeight <= 0) return;` guard checks the **incoming parameters** and returns *before* `framebufferWidth_`/`framebufferHeight_` (`RendererPostProcess`'s own now-separate copies) get assigned — differing from the original, which checked the already-current shared member directly. Traced whether this creates observable staleness: it doesn't — those members are `private` to `RendererPostProcess`, read only synchronously inside the same function right after being assigned, and there's no external getter exposing them. The guard-then-assign vs. assign-then-implicit-guard difference is invisible from outside the class. Verified equivalent by tracing, not just by inspection.
* **`renderer_screenshot.h`:** `PrepareScreenshotReadback`→`PrepareReadback` and `WriteScreenshotPng`→`WritePng` are, likewise, **byte-for-byte identical** — same row-padding/stride math (`AlignTo`, `kScreenshotBytesPerPixel=4`, `kCopyBytesPerRowAlignment=256`), same BGRA↔RGBA channel swap loop, same `MapAsync` + `ProcessEvents`/`Tick`/1ms-sleep polling loop, same `Fail()` messages verbatim (e.g. "Surface does not support CopySrc...", "Cannot capture an empty surface texture", "Screenshot readback map failed", "Unsupported surface format for screenshot capture", "stbi_write_png failed for screenshot path: ..."), same `stbi_write_png` call and console log line. `RendererScreenshotCapture` went further than `RendererPostProcess` on this extraction: it's **fully stateless** — every method is `static`, taking `device`/`instance`/`surfaceFormat`/`surfaceSupportsCopySrc` as explicit parameters rather than holding any of its own. Called as `RendererScreenshotCapture::PrepareReadback(...)`/`::WritePng(...)`, never instantiated — there is no object lifetime question for this class at all.
* **Call-order preservation, the specific thing this audit needed to protect:** traced the full body of the new `RenderFrame()` and confirmed the sequence is unchanged from the just-verified-correct state: star pass → dust pass (`postProcess_.HdrSceneView()` as target, replacing direct `hdrSceneView_` member access — same view, same semantics) → nebula pass → `postProcess_.RunPasses(encoder, backbufferView)` → `RendererScreenshotCapture::PrepareReadback(...)` (still *before* the ImGui pass, still *before* `encoder.Finish()`/`queue_.Submit()`/`Present()` — the screenshot-ordering fix from earlier tonight is fully intact, not regressed) → ImGui pass → `Finish`/`Submit` → `Present()` → (on success) `RendererScreenshotCapture::WritePng(...)`. Every conditional (`dustGaussianCount > 0`, `nebulaGaussianCount > 0`, the ImGui scissor guard, the `Outdated`/`Lost`/`Timeout` surface-status switch) is present, unchanged, in the same order.
* `postProcess_.CreatePipelines(device_, surfaceFormat_)` is called at the same point (end of `CreateRenderPipelines()`) and `postProcess_.CreateTextures(device_, framebufferWidth_, framebufferHeight_)` at the same point (`ConfigureSurface()`, immediately after `CreateDepthTexture()`) as the original — meaning the bloom-audit's already-verified resize-correctness reasoning (every resize path funnels through `ConfigureSurface()`, which recreates the HDR/bright/blur/bloom textures and rebuilds bind groups) transfers over unchanged; this refactor doesn't touch resize logic at all, just where the code that runs during it lives.

## 2. ODR/includes and ownership — no double-ownership, no dangling lifetimes

Both new headers are `#pragma once`-guarded with minimal, correct includes (`renderer_postprocess.h`: `<array>`, `webgpu_cpp.h`, `shaders_generated.h`; `renderer_screenshot.h`: standard headers plus `webgpu_cpp.h` and the same `extern "C" int stbi_write_png(...)` forward-declaration the original used — signature unchanged, so no ODR risk against `third_party/glfw/deps/stb_image_write.h`'s real declaration, already verified compatible in an earlier audit). `renderer.h` includes both new headers immediately after its own project includes — no circular-include risk (neither new header includes `renderer.h` back).

**Ownership:** `Renderer` now holds exactly one `RendererPostProcess postProcess_;` value member — confirmed `hdrSceneTexture_`, `hdrSceneView_`, `brightPassTexture_`/`View_`, `blurTempTexture_`/`View_`, `bloomTexture_`/`View_`, both bind-group layouts, all four postprocess pipelines, and all four postprocess bind groups are **entirely absent** from `Renderer`'s own member list now — they live solely in `RendererPostProcess`, not duplicated. `RendererScreenshotCapture` has no member in `Renderer` at all (stateless, per above) — nothing to double-own. `surfaceSupportsCopySrc_` correctly stays a `Renderer` member (it's a surface-level capability flag, not postprocess- or screenshot-specific) and is passed *by value* into the screenshot functions — no aliasing/reference-lifetime risk. Because `postProcess_` is a plain value member (not a pointer), its lifetime is automatically exactly `Renderer`'s lifetime — not shortened or extended by the refactor. `Renderer::Shutdown()` calls `postProcess_.Shutdown()` at the same point the old inline teardown ran, and its body is (again) byte-for-byte identical to the block it replaced.

## 3. Fresh build — confirmed clean, independently

Ran my own fresh `cmake -S . -B build-native` + full `cmake --build build-native -j$(nproc)` in this isolated worktree (not just trusting the implementer's or director's report): clean, zero warnings, all targets built successfully. No `CMakeLists.txt` changes were needed for the split (both new files are header-only, pulled in via `#include`, no new translation unit to register) — consistent with the diffstat showing only the three `.h` files touched.

## 4. `tests/aspect_ratio_clamp_test.cpp` — verified myself directly, and it was never actually at risk

Compiled and ran it standalone (`g++ -std=c++20 -O2 -I. tests/aspect_ratio_clamp_test.cpp`, no CMake target exists, matching the implementer's claim): **exit 0, "Overall result: SANE (all tests passed)"** across all four width/height cases including the pathological `1×800` and `0×800` ones. Worth noting explicitly: this test only `#include`s `galaxy_system.h` and `vec_math.h` — it never includes `renderer.h`/`renderer_postprocess.h`/`renderer_screenshot.h` at all, and none of those files changed in this commit. So this test wasn't just re-verified passing, it was **never at risk** from this refactor in the first place — a good sanity check to run, but not one that could have caught a `renderer.h`-split regression. (As a nice side effect, its output independently re-confirms the emission-nebulae audit's hand-computed node count again: "leaves=1800 nodes=2403", matching.)

## 5. Line count — confirmed

`wc -l`: `renderer.h` is **1032 lines**, `renderer_postprocess.h` is 244, `renderer_screenshot.h` is 165 — matches the claim exactly, and 1032 is comfortably under the stated 1250-line watchdog limit.

## 6. Display environment

Not applicable and not available (checked anyway for consistency with prior audits: `DISPLAY`/`WAYLAND_DISPLAY`/`XDG_RUNTIME_DIR` empty, no `xdotool`/`kwin_wayland`). This audit is pure structural/build verification and doesn't need a rendered frame to be complete — a code-motion refactor with no behavior change has nothing new to see visually that the prior bloom/dust/nebula audits didn't already flag as needing a look.

## 7. Verdict

**Clean.** This is a genuine, careful, behavior-preserving extraction — verified by direct line-by-line comparison against the pre-refactor code, not by pattern-matching or trusting the commit message. The one non-literal adaptation (the `CreateTextures` guard-before-assign reordering) was traced through and confirmed to have no observable effect. No ODR issues, no double ownership, no lifetime risk — the two new classes compose cleanly into `Renderer`, one by value with matching lifetime, one fully stateless with no lifetime to worry about at all. Build verified clean myself. The aspect-ratio test passes but was never actually exposed to this change. Line count confirmed under the watchdog limit. Nothing here needs a screenshot to be called done — this one really is fully clean, not "clean pending a visual check" like the last three.

---

# Validator Audit Report — Color-only pass pipeline attachment fix

**Methodology:** Static, exhaustive pipeline↔pass attachment trace. No GPU driver available in this container, so this is compile-time/logic verification only — I say explicitly below what I can't confirm.
**Branch reviewed:** `fix/postprocess-depth-attachment`, commit `94256b9` "Fix color-only pass pipeline attachments" (1 commit ahead of `main`/`7a3dfe0`), reviewed in an isolated worktree at `/home/eric/Projects/pgu-attach-audit` (detached HEAD — the branch itself was already checked out in the shared main working directory, see §6). Diff: `main.cpp` +1/-1, `renderer.h` +8/-5, `tuning_panel.h` +2/-3.
**Status:** **Clean.**

## 1. Every render pipeline vs. its render pass — traced exhaustively

Went through every `BeginRenderPass`/pipeline pair in `RenderFrame()` (renderer.h) and `RunPasses()` (renderer_postprocess.h):

| Pass | Color format(s) | Depth attachment? | Pipeline used | Pipeline's declared depthStencil | Match? |
|---|---|---|---|---|---|
| Star (scene) pass, L557-593 | `RGBA16Float` (`postProcess_.HdrSceneView()`) | Yes, `depthView_` (`Depth24Plus`) | `hardwarePointPipeline_` or `splatAdditivePipeline_`/`splatAlphaPipeline_` | `Depth24Plus` (`hardwareDepthStencil`/`splatDepthStencil`) | ✅ |
| Dust pass, L595-613 | `RGBA16Float` | No | `dustExtinctionPipeline_` | none (`pipelineDescriptor.depthStencil` never set, L419-429) | ✅ (pre-existing, untouched by this diff) |
| Nebula pass, L615-633 | `RGBA16Float` | No | `nebulaAdditivePipeline_` | none, **as of this fix** (`createSplatPipeline(&additiveBlend, nullptr)`) | ✅ now — was ❌ before (pipeline declared `Depth24Plus`, pass bound no depth texture — this was the crash) |
| Bright-extract / blur-H / blur-V, renderer_postprocess.h L76-78 | `RGBA16Float` each | No | `brightExtractPipeline_`/`blurHorizontalPipeline_`/`blurVerticalPipeline_` | none (`CreateFullscreenPipeline` never sets `depthStencil`) | ✅ (untouched by this diff) |
| Tonemap (composite), L79 | `surfaceFormat_` (matches `backbufferView`) | No | `tonemapPipeline_` | none | ✅ (untouched) |
| ImGui pass, L664-676 | `surfaceFormat_` (`backbufferView`) | No (`imguiPassDescriptor` has no `depthStencilAttachment`) | ImGui WGPU backend's internal pipeline, built from `RenderTargetFormat`/`DepthStencilFormat` passed to `InitImGui` | `RenderTargetFormat = SurfaceFormat()`, `DepthStencilFormat = Undefined`, **as of this fix** | ✅ now — was ❌ before (`DepthStencilFormat` was `renderer_.DepthFormat()` = `Depth24Plus`, but the ImGui pass never bound a depth texture) |

All six color-format matches were already correct before this fix (color target formats were never the issue). `sampleCount` is `1` everywhere — every texture descriptor (`depthDescriptor`, `CreateHdrTexture`, the surface config) and every pipeline's `multisampleState` hard-codes `count = 1`; no MSAA path exists anywhere in this codebase, so there's no sample-count mismatch to check for.

**Confirmed: no second latent mismatch.** The two pairs the fix touched (nebula pipeline↔pass, ImGui pipeline↔pass) are the only two that were ever wrong — every other pipeline/pass pair was already consistent, including the four postprocess passes and the dust pass, none of which this commit touches.

## 2. Nebula depth removal — safe, and the pass never had depth to begin with

Checked whether nebula was ever meaningfully depth-tested against stars before this fix, since removing a depth test silently changing draw order/occlusion would be a real regression. It wasn't, for a stronger reason than "nebula is additive so order doesn't matter": **the nebula render *pass* (`nebulaPassDescriptor`, L621-623) has never bound a depth attachment at all, before or after this commit** — that code is untouched by this diff. Only the *pipeline's* declared `depthStencil` state changed (from `&splatDepthStencil` to `nullptr`). Since Dawn's validation requires a pipeline's depth/stencil declaration to match the bound pass, the pre-fix combination (pipeline says "I have a Depth24Plus attachment", pass says "no depth attachment bound") would have failed pass creation/draw validation immediately — meaning nebula never successfully depth-tested against anything at runtime in the pre-fix code; it just crashed. There is no prior "nebula occluded correctly behind stars via depth test" behavior being removed here, because that behavior never worked in the first place. The fix is purely bringing the pipeline's declaration in line with a pass shape that was already fixed.

Separately, even had the pass carried a depth attachment, `additiveBlend` (`srcFactor=One, dstFactor=One, Add` on both channels) is genuinely commutative/associative, and `splatDepthStencil` — when it *is* used, e.g. for `splatAdditivePipeline_`/`splatAlphaPipeline_` in the star pass — sets `depthWriteEnabled = false`, so even the surviving depth-tested splat pipelines only *read* depth (test against the hardware-point pipeline's writes), never write it themselves. So the "was nebula relying on being depth-tested against stars" question has a firm no on two independent grounds: the pass never bound depth, and the additive math wouldn't have depended on draw order even if it had.

## 3. ImGui depth removal — confirmed correct, no other consumer

`imguiPassDescriptor` (renderer.h L669-671) sets only `colorAttachmentCount = 1`/`colorAttachments = &imguiColorAttachment` — no `depthStencilAttachment` field is populated (it's default/null on `wgpu::RenderPassDescriptor`). So `WGPUTextureFormat_Undefined` is the only correct value for `initInfo.DepthStencilFormat` in `InitImGui`; the pre-fix `renderer_.DepthFormat()` (`Depth24Plus`) was simply wrong for a pass that has never included a depth buffer. Grepped for other `InitImGui`/`DepthFormat()` call sites (`select:Grep` over `*.cpp`/`*.h`): `InitImGui` has exactly one call site (`main.cpp:282`, updated correctly to drop the now-removed parameter), and `DepthFormat()` — the `Renderer` accessor that used to feed it — has no remaining callers at all; it's now dead code (harmless, but worth a follow-up cleanup, not a correctness issue and not something I'd block this fix on).

## 4. Build — clean

Fresh `cmake -S . -B build` + `cmake --build build -j32` in the isolated worktree: configures and links with **exit 0**, no warnings surfaced in the build log. This confirms the fix is at minimum syntactically/type correct; it does **not** confirm the fix resolves the reported Dawn validation crash at runtime — no GPU driver in this container (checked: no `DISPLAY`/`WAYLAND_DISPLAY`, no Xwayland), so the actual "does the app now run past frame 1 instead of crashing" behavior is unverified and needs a real display environment.

## 5. Minimality — confirmed targeted, one pre-existing loose end noted

The diff is exactly the two described changes: `createSplatPipeline`'s lambda gained a second parameter used only to give `nebulaAdditivePipeline_` a `nullptr` depth state (the other two call sites pass `&splatDepthStencil`, unchanged in effect), and `InitImGui`/its one call site dropped the depth-format parameter in favor of a hard-coded `Undefined`. No renaming, no reflow of unrelated lines, no touched files beyond the three needed. The one loose end is the now-dead `Renderer::DepthFormat()` accessor (§3) — leftover from the old plumbing, not cleaned up in this commit. Doesn't affect correctness and I wouldn't hold up the merge for it, but flagging so it doesn't get forgotten.

## 6. Process note (not a fix-correctness issue)

The shared main working directory (`/home/eric/Projects/pgu`) is currently checked out **on `fix/postprocess-depth-attachment` itself**, not `main` — someone worked directly in the shared checkout instead of a worktree. I created my isolated worktree via `git worktree add` as instructed regardless (it came up as a detached `HEAD` at `94256b9` since the branch was already checked out elsewhere), so this audit itself is unaffected, but the shared checkout being off `main` is worth the director's attention independent of this fix's merge.

## 7. Verdict

**Clean, no second latent attachment mismatch.** Both changes are correct and match the pass shapes they bind to; exhaustively traced every pipeline/pass pair in the renderer (scene, dust, nebula, all four postprocess passes, ImGui) and confirmed all are now consistent, with the two that were fixed being the only two that were ever wrong. Nebula's depth removal is safe on two independent grounds (the pass never bound depth at all, and the additive blend is order-independent regardless). ImGui's depth removal is correct given its pass genuinely has no depth attachment, with no other call site depending on the old behavior. Build is clean. Fix is minimal and targeted, modulo one harmless dead accessor left behind. **Can't confirm:** whether this actually eliminates the reported runtime Dawn validation crash — no display/GPU driver in this container to run the app and observe. Recommend merging to `main` on the strength of the static trace (this is unambiguously a real bug being fixed, and the "does it actually stop crashing" question is best answered by whoever next runs it with a real GPU, ideally as the very first thing checked after the next image rebuild). Also flagging the shared-checkout branch state (§6) separately from this fix's own correctness.

---

# Validator Audit Report — kMaxCutGaussians 16384→65536 + bloom re-tune

**Methodology:** Focused static audit on the one part of a tuning change with real correctness stakes (a fixed-capacity constant bump), plus a lower-risk shader-literal sanity check. Reviewed in an isolated worktree at `/home/eric/Projects/pgu-sharp-audit` (detached HEAD — as with the previous audit, the branch was already checked out directly in the shared main working directory rather than a worktree; see the standing note in the prior report).
**Branch reviewed:** `tune/fidelity-sharpness`, commit `8a9c5a2` "Tune galaxy sharpness". Diff confirmed exactly 2 files via `git diff origin/main...8a9c5a2 --stat`: `gpu_types.h` (+1/-1), `shaders/postprocess.wgsl` (+3/-3) — matches the director's description exactly, nothing else snuck in.
**Status:** **Clean.**

## 1. Every use of `kMaxCutGaussians` — all scale off the constant, no hardcoded 16384, no uint16 landmine

`grep -rn "kMaxCutGaussians"` across the whole tree finds exactly these consumers, all of which read the constant rather than a copied literal:

* `gpu_types.h:17` — the definition itself, now `65536`.
* `galaxy_system.h:31-32` — `pointPositions_.resize(kMaxCutGaussians)` / `pointStaticAttributes_.resize(kMaxCutGaussians)` in the `GalaxySystem` constructor: the two CPU-side per-frame cut vectors, sized directly off the constant.
* `galaxy_system.h:740-741` — `SelectCut`'s fixed-capacity guard: `if (cutGaussianCount_ >= kMaxCutGaussians) return;` before any write. Traced `EmitGaussian` (galaxy_system.h:908-922): it writes to `pointPositions_[out]`/`pointStaticAttributes_[out]` where `out = cutGaussianCount_` *before* incrementing — so the guard in `SelectCut` is the only thing standing between this write and an out-of-bounds index, and it correctly gates on the same constant the vectors were sized to. No separate/duplicated bound anywhere.
* `renderer.h:154,159` — `positionBufferDescriptor.size`/`staticBufferDescriptor.size`, the two GPU vertex buffers, also sized directly off the constant (`static_cast<uint64_t>(kMaxCutGaussians) * sizeof(...)`).

Every consumer traces back to the one constant — raising it in `gpu_types.h` keeps all four allocation sites (2 CPU vectors, 2 GPU buffers) and the one guard consistent by construction; there's no fifth place to miss.

**Hardcoded-16384 check:** `grep -rn "16384"` across the project source (excluding `third_party/`) returns **zero hits** — the only `16384` matches anywhere in the tree are inside `third_party/glfw`, `third_party/imgui`'s vendored `stb`/`nuklear` code, completely unrelated constants (a zlib hash table size, Vulkan flag bit values, TrueType font unit scaling). Nothing project-side hardcodes the old cap.

**uint16 check — this was the specific landmine to check for, and it's absent:** `grep -rn "uint16_t\|uint16\b\|u16\b"` across every project header/source/shader/test file returns **zero hits**. Every gaussian-related count and index in the codebase — `cutGaussianCount_`, `dustGaussianCount_`, `nebulaGaussianCount_`, the `CutGaussianCount()`/etc. accessors, `GaussianNode::childStart`/`childCount`, every loop index touching the hierarchy or the cut arrays — is `uint32_t` (confirmed by reading the full declaration list in `galaxy_system.h` and `gpu_types.h`, not just grepping the word "count"). `65536` fits `uint32_t` with vast headroom (max ~4.29 billion); there's no off-by-one-at-a-power-of-two risk here because nothing narrower than 32 bits is in the gaussian-count path at all.

**No workgroup/dispatch assumption:** grepped for `Dispatch`/`workgroup`/`@compute`/`ComputePipeline` across the whole project tree — zero hits outside `third_party`. This renderer has no compute shaders; the LOD cut is built entirely CPU-side (`SelectCut`, confirmed WebGPU-agnostic per its own header comment) and uploaded as a plain instanced vertex-buffer draw (`pass.Draw(6, cutGaussianCount)` in `renderer.h`'s `RenderFrame`). There is no dispatch grid or workgroup size anywhere that could have been silently tuned for the old 16384 cap.

## 2. Memory/perf sanity — trivial at 4x

`sizeof(PointPosition) == 12` and `sizeof(PointStaticAttributes) == 36` (both `static_assert`-enforced in `gpu_types.h`, so these can't silently drift from the GPU layout). At the new cap:
* GPU position buffer: `65536 × 12 = 786,432` bytes (768 KiB).
* GPU static-attribute buffer: `65536 × 36 = 2,359,296` bytes (2.25 MiB).
* Matching CPU-side vectors: same two sizes again (~3 MiB total CPU + ~3 MiB GPU).

Combined, well under 6 MiB total for both cap-driven allocations doubled across CPU/GPU — four orders of magnitude below any default WebGPU/Dawn buffer-size limit (defaults are in the hundreds-of-MB range; no `requiredLimits` are requested anywhere in `InitDevice` that could clamp this lower). This is not a meaningful allocation by any GPU's standards. Perf-wise, the director's own telemetry (21,792 gaussians actually drawn at the test view) is well under even the *old* 16,384 cap's neighborhood at 1.3x — the 65536 ceiling is headroom, not something the renderer will realistically saturate at typical view distances; cost scales with gaussians actually emitted (tree-cut traversal cost is bounded by rendered gaussians per the class's own doc comment), not the cap.

## 3. Bloom shader literals — confirmed to be the only change, and numerically sane

`git diff origin/main...8a9c5a2 -- shaders/postprocess.wgsl` touches exactly three numeric literals, nothing structural: `fs_bright_extract`'s `threshold` (1.0→1.5) and `knee` (0.35→0.2), and `fs_tonemap`'s `bloom_strength` (0.45→0.22). Traced both functions in full:
* `fs_bright_extract`: `soft = clamp((luminance - threshold + knee) / (2.0 * knee), 0.0, 1.0)` — `knee = 0.2` is still strictly positive, so `2.0 * knee = 0.4` in the denominator is safe, no div-by-zero. `contribution = max(luminance - threshold, 0.0) + soft*soft*knee` and `scale = contribution / max(luminance, 1.0e-4)` — the `1.0e-4` floor on the luminance divisor is untouched by this diff and still guards that division independently of the threshold/knee values.
* `fs_tonemap`: `bloom_strength` is a plain multiplicative scale (`scene + bloom * bloom_strength`) with no division at all — there's no way a smaller positive value here introduces a div-by-zero or numerical instability; it just scales bloom contribution down, consistent with the stated "less haze" intent.

No other lines in the shader changed — confirmed via the stat above (+3/-3, matching exactly the three literal edits, no stray whitespace/logic touched).

## 4. Build — clean

Fresh `cmake -S . -B build` + `cmake --build build -j32` in the isolated worktree: **exit 0**, no warnings surfaced, all targets linked successfully.

## 5. Scope/minimality

Diff is exactly the two described numeric changes across two files — no restructuring, no touched tests (grepped `tests/` for `kMaxCutGaussians`/`16384`/`65536`: no test depends on or asserts against this constant, so none needed updating and none are at risk of now-stale expectations).

## 6. Verdict

**Clean.** The `kMaxCutGaussians` bump is the only part of this change with real correctness stakes, and it checks out completely: every one of its four allocation sites plus the one bounds-check guard scale off the single constant, there is no hardcoded `16384` anywhere in project source, and — the specific landmine the director flagged — **there is no `uint16_t`/narrower-than-32-bit gaussian index or count anywhere in the codebase**, so `65536` (which does exceed `uint16_t`'s range by one) has nothing to overflow into. No compute dispatch/workgroup sizing exists at all in this renderer, so that class of assumption doesn't apply here. Memory impact is trivial (~3 MiB CPU + ~3 MiB GPU total, four orders of magnitude under any realistic limit). The bloom shader edits are confirmed to be exactly the three stated literals, all numerically safe (no new div-by-zero, knee/threshold both still positive). Build is clean. Recommend merging to `main`.

---

# Validator Audit Report — nebula/dust cap bumps + leaf counts + shader re-tune

**Methodology:** Focused correctness-only static audit (same class of check as the `kMaxCutGaussians` bump) — the director has already made the aesthetic call from rendered output; this pass verifies only the mechanical/sizing changes are safe. Reviewed in an isolated worktree at `/home/eric/Projects/pgu-fid-audit` (detached HEAD at the branch tip).
**Branch reviewed:** `tune/fidelity-nebula-dust`, commits `3a230dc` ("Tune nebulae as smaller HII knots") and `0ed5bd0` ("Tune dust toward filamentary lanes"). Full diff vs `origin/main` confirmed via `git diff origin/main...HEAD --stat`: exactly 3 files — `galaxy_system.h` (+30/-28... net widened by the diff stat tool's line-splitting, all in the constants/generation functions), `gpu_types.h` (+4/-2, the two cap bumps), `shaders/dust_extinction.wgsl` (+8/-8, the fBM frequency/smoothstep/mix constants). No files outside this set touched.
**Status:** **Clean.**

## 1. `kMaxNebulaGaussians` (2048→4096) and `kMaxDustGaussians` (4096→8192) — every consumer scales off the constant, no narrower type anywhere

`grep -rn "kMaxNebulaGaussians\|kMaxDustGaussians"` (excluding `third_party/`) finds exactly these consumers, none of which copy a literal:

* **Nebula** — `galaxy_system.h:36-37`: `nebulaPositions_.resize(kMaxNebulaGaussians)` / `nebulaStaticAttributes_.resize(kMaxNebulaGaussians)` (2 CPU vectors); `galaxy_system.h:805`: `SelectNebulaCut`'s guard `if (nebulaGaussianCount_ >= kMaxNebulaGaussians) return;`; `renderer.h:175,180`: the two GPU buffer descriptor sizes. Traced `EmitNebulaGaussian` (galaxy_system.h:1022 onward): writes to `nebulaPositions_[out]`/`nebulaStaticAttributes_[out]` at `out = nebulaGaussianCount_` before incrementing — same safe pattern as the already-verified star path, gated by the one guard.
* **Dust** — `galaxy_system.h:33-35`: `dustPositions_.resize(kMaxDustGaussians)` / `dustStaticAttributes_.resize(kMaxDustGaussians)` / **`dustSortDepths_.resize(kMaxDustGaussians)`** (3 CPU vectors — dust has one more array than nebula/stars, for its depth-sort field; confirmed it's sized off the same constant, not a separately-tracked capacity); `galaxy_system.h:775`: `SelectDustCut`'s guard; `renderer.h:165,170`: the two GPU buffer descriptor sizes. Traced `EmitDustGaussian` (galaxy_system.h:964 onward): writes `dustPositions_[out]`/`dustStaticAttributes_[out]`/`dustSortDepths_[out]` all at the same `out = dustGaussianCount_`, then increments — all three arrays move in lockstep off the one guard and the one constant.

**No hardcoded old values:** `grep -rn "\b2048\b\|\b4096\b\|\b8192\b"` across the whole project tree (excluding `third_party/`) returns **only the two new constant definitions in `gpu_types.h` itself** — nothing else in the tree references `2048`, `4096`, or `8192` as a bare literal. (Note `kMaxNebulaGaussians` is now `4096` — the *same* numeral the old `kMaxDustGaussians` used to be — so this grep needed care to confirm both are the new intended constant assignments and not a leftover cross-reference; confirmed by reading the actual lines, both are exactly `gpu_types.h:18-19`, the two definitions.)

**Type consistency, the specific ask:** re-ran the uint16 grep (`uint16_t\|uint16\b\|u16\b`) across the whole project source — still **zero hits**, unchanged from the prior audit. Every gaussian count/index in both the dust and nebula paths (`dustGaussianCount_`, `nebulaGaussianCount_`, their accessors, the `EmitDustGaussian`/`EmitNebulaGaussian` `out` locals, the hierarchy node `childStart`/`childCount` fields they share with the star path) is `uint32_t`, exactly like `kMaxCutGaussians`'s handling. `4096` and `8192` both fit comfortably in `uint16_t` too (unlike `65536`), so there was never actually a boundary-crossing risk for these two specifically — but the type consistency is real and confirmed: nothing here uses a narrower type than the star path did, so if a future bump ever does cross 65535 for dust or nebula, the same headroom that made the star bump safe already applies.

**Memory, confirmed trivial:** using the already-`static_assert`-fixed sizes (`PointPosition`=12B, `PointStaticAttributes`=36B):
* Nebula: `4096 × (12+36) = 196,608` bytes (192 KiB) total across the CPU+GPU pair.
* Dust: `8192 × (12+36) = 393,216` bytes (384 KiB) GPU-side; CPU-side adds `dustSortDepths_`'s `8192 × 4 = 32,768` bytes (32 KiB) on top.

Combined new total across both bumps: well under 1 MiB — negligible, consistent with the `kMaxCutGaussians` finding.

## 2. Leaf counts vs. caps — `kNebulaLeafCount` (1800→3200) and `kDustLeafCount` (24000→36000): hierarchy build is leaf-count-agnostic to the cap, confirmed by tracing the actual build/traversal split

Nebula's new leaf count (3200) sits under its new cap (4096), so there's no leafCount>cap case there. Dust's new leaf count (36000) sits well **above** its new cap (8192) — this is the case the director specifically flagged, and it's safe for a structural reason, not a coincidence: **the hierarchy-build functions (`BuildDustHierarchyFromSamples`/`BuildNebulaHierarchyFromSamples`) take their `leafCount` from `dust.size()`/`nebulae.size()` and have no reference to `kMaxDustGaussians`/`kMaxNebulaGaussians` anywhere in their bodies** — traced both in full: they Morton-sort all leaves, then bottom-up-merge in fixed groups of 4 (`kMergeFanout`) until exactly one root remains, building a complete tree over however many leaves were generated, with zero dependency on the render-time cap. The cap only enters at the opposite end — the top-down `SelectDustCut`/`SelectNebulaCut` traversal, gated purely by the count guard (§1) and the projected-size-vs-threshold LOD test. So a 36000-leaf tree feeding an 8192-gaussian cap behaves exactly like the already-established star case (60000 leaves against the *pre-bump* 16384 cap, which this same codebase has run with before this whole tuning arc started): the traversal simply stops expanding/emitting once the cap is hit, which can only manifest as *coarser LOD than the view ideally wants* if the view ever demands more detail than the cap allows — a visual-quality question, correctly out of scope for this correctness audit, never a crash or overflow. Confirmed no separate cap-vs-leafCount assertion or assumption exists anywhere that this ordering could violate.

## 3. Other tuning constants — sanity-checked, all sane

Read every changed numeric constant and traced its use:

* **Nebula generation** (`galaxy_system.h` inside `GenerateNebulaSamples`): `coreSigma = (0.018 + 0.062·sizeDraw²) × max(0.8, scaleRadius/3.2)` — `sizeDraw ∈ [0,1]` (from the existing `random01`), so `sizeDraw² ∈ [0,1]`, coreSigma strictly positive, range `[0.018, 0.08]×factor` (previously `[0.05, 0.16]×factor` — smaller as intended for "smaller HII knots", still positive). `verticalSigma = max(0.012, thickness·0.08)` — floor keeps it positive regardless of `thickness`. The second covariance axis scale `(0.55 + 0.75·rand)` and `brightness = (0.45+1.45·rand)·(0.35+0.65·normalizedArm)` are both strictly positive over `rand, normalizedArm ∈ [0,1]` (the latter already `std::clamp`-ed earlier in the same function, unchanged by this diff). `kNebulaAmplitudeScale` (1.15e-5, was 2.4e-5) is a positive multiplier in `EmitNebulaGaussian`'s `amplitude = kNebulaAmplitudeScale · brightness / (2π·sqrtDet)` — it's in the numerator, not the divisor, so a smaller value can't introduce a division problem; the divisor `sqrtDet` has its own pre-existing `1.0e-14f` floor via `e1`/`e2`, untouched by this diff. `kNebulaPixelErrorThreshold` (0.020, was 0.028) stays strictly positive — it's compared against, never divided by.
* **Dust generation** (`GenerateDustSamples`): `longSigma`/`shortSigma`/`verticalSigma` all follow the identical positive-by-construction pattern as nebula's (positive base + positive random term, or a `max()` floor) — just smaller coefficients for thinner lanes, still strictly positive. `opticalDepth = (0.036+0.14·rand)·(0.30+0.85·normalizedArm)` strictly positive. `kDustOpticalDepthScale` (0.24, was 0.18) is a positive multiplier feeding `attr.amplitude = std::min(brightness·kDustOpticalDepthScale, 2.5f)` — the `std::min` ceiling (untouched) still caps it, no overflow risk from the larger scale. `kDustPixelErrorThreshold` (0.09, was 0.14) stays positive, compared-against only.
* **`shaders/dust_extinction.wgsl`**: the fBM frequency multipliers (`11.0`, `36.0`, was `7.5`/`23.0`) only rescale the *input* to `fbm()` — traced `fbm()`'s body: its `norm` accumulator is a fixed geometric sum over exactly 5 fixed-amplitude octaves (`0.5, 0.25, 0.125, 0.0625, 0.03125`, summing to `0.96875`), entirely independent of the caller's frequency scaling, so `sum / norm` can never divide by zero regardless of what frequency the caller passes — confirmed this is unconditionally safe, not just safe for these two specific values. The `smoothstep(0.50, 0.82, ridge_noise)` edges (was `0.40, 0.88`) still satisfy `edge0 < edge1`, required for `smoothstep` to behave (and even if reversed, WGSL's `smoothstep` doesn't trap — but the ordering is correct here regardless). The final blend coefficients (`0.35+0.65·fine_noise`, `0.16+2.05·filament`) are positive-by-construction sums, and `transmittance = exp(-extinction·optical_depth)` has no division at all — a larger `optical_depth` just makes `exp` approach 0 (fully opaque), never NaN/Inf from below.

## 4. Build — clean

Fresh `cmake -S . -B build` + `cmake --build build -j32`: **exit 0**, no warnings, all targets linked.

## 5. Scope

Confirmed via `git diff origin/main...HEAD --stat`: exactly the 3 files described (`galaxy_system.h`, `gpu_types.h`, `shaders/dust_extinction.wgsl`) — no test files reference any of the changed constants (`kMaxNebulaGaussians`/`kMaxDustGaussians`/`kNebulaLeafCount`/`kDustLeafCount`/the old numeric literals), so nothing is at risk of a stale assumption elsewhere.

## 6. Verdict

**Clean.** Both raised caps (nebula 2048→4096, dust 4096→8192) have every consumer — CPU vector resizes (2 for nebula, 3 for dust including the dust-only sort-depths array), the per-path bounds guard, and the GPU buffer descriptor sizes — scaling off the single constant each, with no hardcoded old value anywhere in project source and no narrower-than-`uint32_t` type in either path (consistent with how `kMaxCutGaussians=65536` was already handled; these two specifically never crossed the `uint16_t` boundary anyway, but the type discipline holds regardless). The dust `leafCount(36000) > cap(8192)` case — the one flagged as needing explicit confirmation — is handled safely because the hierarchy-build step has no dependency on the render cap at all; the cap only gates the top-down cut-selection guard, exactly mirroring the pre-existing star path's already-proven pattern. All other changed tuning constants (nebula/dust generation coefficients, dust shader fBM frequencies and smoothstep edges) were traced individually and are sane: strictly positive where required, no new division introduced, `fbm()`'s normalization is frequency-independent so no div-by-zero risk from the frequency changes. Build is clean. Recommend merging as a fidelity checkpoint.

---

# Validator Audit Report — coherent dust lanes (new generation code) + dust cap bump + H-alpha pop lift

**Methodology:** Correctness-only static audit of new procedural-generation code, plus the same buffer-sizing landmine check as the two prior cap-bump passes. The director made the aesthetic call from rendered output already; this pass is purely: does the new math ever produce NaN/Inf/garbage or crash, and is it deterministic. Reviewed in an isolated worktree at `/home/eric/Projects/pgu-lanes-audit` (detached HEAD at branch tip).
**Branch reviewed:** `tune/fidelity-dust-lanes`, commits `8ca1f4e` ("Coherent dust lanes in arm space") and `0a13f9e` ("Lift H-alpha knot pop"). Full diff vs `origin/main` confirmed via `git diff origin/main...HEAD --stat`: 3 files — `galaxy_system.h` (+84/-15, the new lane-generation code plus constant tuning), `gpu_types.h` (+1/-1, the cap bump), `shaders/dust_extinction.wgsl` (+4/-4, fBM frequency/smoothstep/blend retune). No other files touched.
**Status:** **Clean.**

## 1. `kMaxDustGaussians` (8192→12288) — same pattern as the prior two cap audits, still holds

`grep -rn "kMaxDustGaussians"` (excluding `third_party/`): definition (`gpu_types.h:18`), the 3 CPU vector resizes including the dust-only `dustSortDepths_` (`galaxy_system.h:33-35`), the `SelectDustCut` bounds guard (`galaxy_system.h:839`, `if (dustGaussianCount_ >= kMaxDustGaussians)`), and the 2 GPU buffer descriptor sizes (`renderer.h:165,170`) — every consumer unchanged in shape from the last two audits, still all scaling off the one constant. `grep -rn "\b8192\b\|\b12288\b"` across the whole tree (excluding `third_party/`) finds **only the one definition line** — no hardcoded old value anywhere. Re-ran the `uint16_t`/`uint16`/`u16` grep across the whole project source: still **zero hits**. `12288` is comfortably `uint32_t`-safe with enormous headroom, consistent with the established pattern.

**Dust `leafCount(48000) > cap(12288)`:** `kDustLeafCount` only feeds the loop bound and `out.reserve()` in `GenerateDustSamples` (`galaxy_system.h:372,374`); `BuildDustHierarchyFromSamples` (unchanged by this diff, already verified in the prior audit) takes its `leafCount` from `dust.size()` and has zero reference to `kMaxDustGaussians` anywhere in its body — it always builds the complete bottom-up-merge tree over however many leaves were generated. The cap only gates `SelectDustCut`'s guard at the opposite end of the pipeline. Same structural independence verified twice before now holds a third time at a new ratio (48000:12288, roughly 3.9:1, similar order to the previous 36000:8192 ≈ 4.4:1) — nothing about this diff touches that separation.

## 2. New procedural-generation code — traced every function for NaN/Inf, div-by-zero, degenerate input, and determinism

Five new `static` (pure, no instance state) member functions were added to `GalaxySystem`, in call order from leaf to top:

* **`SmoothStep(edge0, edge1, x)`** (`galaxy_system.h:245-248`): `t = clamp((x-edge0) / max(edge1-edge0, 1.0e-6f), 0, 1)`. The `max(..., 1.0e-6f)` floor on the denominator means **even a degenerate `edge0 == edge1` call can't divide by zero** — it falls back to dividing by `1e-6`, producing a large-but-finite ratio that the surrounding `clamp` immediately saturates to 0 or 1 depending on the sign of `x - edge0`. This matters concretely: one call site is `SmoothStep(comp.scaleRadius * 0.22f, comp.scaleRadius * 0.75f, r)` (the `coreEase` term) — if a component ever had `scaleRadius == 0`, both edges collapse to `0.0f` and this is exactly the degenerate case the floor protects against. Verified safe either way.
* **`ValueNoise2D(seed, x, y, stream)`** (`galaxy_system.h:250-267`): `floor`/`fract` cell lookup, bilinear-lerped between four `random01()` hash samples keyed by a packed `(gx,gy)` cell coordinate. No division, no log/sqrt. `floor`/cast-to-`int` are well-defined for any finite `x`/`y` (the only inputs this function ever receives, traced back to finite `radius`/`thetaPitch` products — never themselves NaN/Inf since they derive from `SampleSersicRadius` output and finite angle values, unchanged surrounding code). `random01` itself (`hash_util.h`) is a pure integer-hash function with no floating-point edge cases — always returns a finite value in `[0,1)`.
* **`Fbm2D(seed, x, y, stream)`** (`galaxy_system.h:269-280`): sums 5 octaves of `amplitude * ValueNoise2D(...)` with `amplitude` starting at `0.5` and halving each octave, divided by `norm` (the same amplitude sum). The `norm > 0.0f ? sum/norm : 0.0f` guard is defensive but actually unreachable as a zero case — `norm` is a fixed geometric sum (`0.5+0.25+0.125+0.0625+0.03125 = 0.96875`) over an unconditional 5-iteration loop, so it's always exactly that positive constant regardless of `x`/`y`/`seed`. Since each `ValueNoise2D` term is in `[0,1)` and the weights `amplitude_i/norm` sum to 1, the result is a proper convex combination — **provably bounded in `[0,1)`**, never a runaway value, regardless of input.
* **`WrappedPhase(phase)`** (`galaxy_system.h:282-284`): `atan2(sin(phase), cos(phase))`. This is the standard robust angle-wrap idiom — `atan2`'s only undefined input is `(0,0)`, and since `sin²+cos²=1` identically, that pair is never both zero. **Safe for any finite `phase`, including arbitrarily large multiples of 2π** (the exact "angle wraparound" case the director asked me to check) — `armPhase = comp.armCount * (angle - thetaPitch)` can exceed `±2π` many times over depending on `armCount`/`thetaPitch`, and `WrappedPhase` folds it back to `(-π, π]` correctly regardless of magnitude.
* **`SpiralDustLaneStrength(seed, componentIndex, comp, radius, angle, thetaPitch)`** (`galaxy_system.h:286-296`): composes the above. `laneWidth = 0.34f` is a fixed literal (never data-dependent, so it can never degenerate to zero) — `laneDistance / laneWidth` is unconditionally safe. `laneMask = exp(-0.5 * ratio²)` is a decaying Gaussian, bounded in `(0, 1]` for any finite `ratio` (never `Inf` — `exp` of a very negative argument underflows toward `0.0`, it doesn't trap). `continuity = 0.5 + 0.5 * SmoothStep(...)` is bounded `[0.5, 1.0]` by construction (`SmoothStep` itself is clamp-bounded `[0,1]`). Final `clamp(laneMask * continuity, 0, 1)` is redundant-but-harmless belt-and-suspenders on an already-bounded product. **Tested the specific degenerate inputs by hand-tracing:** `radius = 0` (only feeds `sin(0*1.15+...)` and `Fbm2D(..., 0.52*0=0, ...)`, both well-defined), and large/wrapped `angle` (handled by `WrappedPhase` as above). No path through this function can produce NaN or Inf for any finite input.
* **Determinism:** every one of these functions is `static`, takes all its state as parameters, and bottoms out in `random01(seed, index, stream)` — a documented "pure function of (seed, index, stream)" (`hash_util.h`'s own comment) with no global/static mutable state, no clock, no incidental ordering dependence. Same `(seed, componentIndex, comp, radius, angle, thetaPitch)` in ⇒ bit-identical output, unconditionally. The call site in `GenerateDustSamples` threads the same per-galaxy `seed = galaxy_.seed ^ 0xD057'1A6Eu` used by every other dust-generation draw in this function (unchanged pattern) — so regenerating with the same `galaxy_.seed` reproduces the same lanes exactly.

**One non-bug efficiency note, not blocking:** in the 14-attempt rejection-sampling loop (`galaxy_system.h:395-406`), `laneStrength` is computed once inside the loop for the winning `proposal`, then recomputed a second time immediately after the loop with `angle` — which, at that point, always equals the same `proposal` the loop just accepted (or the last one tried, if all 14 attempts were rejected). Since `SpiralDustLaneStrength` is a pure function, this second call is provably redundant (same inputs, same output) — wasted work, not a correctness issue, and cheap enough (one `Fbm2D` call per leaf-generation attempt, `kDustLeafCount=48000` total) that it's not worth flagging as more than a minor cleanup opportunity.

## 3. Elongated-along-arm-tangent covariance and `coreEase` — sane, unchanged risk surface

The `alongArm`/`acrossArm`/`radial`/`angular`/`spiralB` construction feeding `CovarianceFromAxes` is **unchanged by this diff** (confirmed via the diff hunk boundaries — only `longSigma`/`shortSigma`/`coreEase`/`opticalDepth` lines were touched; the vector-basis lines above them are identical context, already exercised by two prior fidelity passes without issue). The new `coreEase = 0.72f + 0.28f * SmoothStep(scaleRadius*0.22, scaleRadius*0.75, r)` is bounded to exactly `[0.72, 1.0]` by construction (`SmoothStep` output is clamp-bounded `[0,1]`) — matches the director's stated `0.72..1.0` range exactly, verified rather than assumed. `longSigma`/`shortSigma` retain the pre-existing positive-by-construction pattern (positive base + positive random term × a `max(0.8, ...)` floor) — still strictly positive, just different coefficients for tighter lanes.

## 4. Nebula pop constants — sane

`kNebulaAmplitudeScale` (1.15e-5→1.35e-5): positive multiplier in the numerator of `EmitNebulaGaussian`'s amplitude formula (unchanged divisor, already-floored `sqrtDet`) — larger value just brightens, no division risk. `color = ClampColor({1.0, 0.22+0.10·blueLift, 0.46+0.24·blueLift})` for `blueLift ∈ [0,1]`: still `R=1.0` fixed, `G ∈ [0.22,0.32]`, `B ∈ [0.46,0.70]` — `B` still consistently exceeds `G` without reaching `R`, so this stays on the same pink/rose side of the color wheel as before (more saturated: `G` shrunk, `B` shifted slightly, matching "lift saturation" intent), passed through the pre-existing `ClampColor` helper unchanged. `brightness = (0.55+1.65·rand)·(0.38+0.68·normalizedArm)` strictly positive over `rand, normalizedArm ∈ [0,1]` (the latter still `std::clamp`-ed earlier, untouched).

## 5. Build — clean

Fresh `cmake -S . -B build` + `cmake --build build -j32`: **exit 0**, no warnings, all targets linked.

## 6. Scope

Exactly the 3 files in the diff stat, no test files reference `kMaxDustGaussians`/`kDustLeafCount`/`SpiralDustLaneStrength`/any of the old numeric literals — nothing at risk of a stale assumption elsewhere.

## 7. Verdict

**Clean.** The dust cap bump (8192→12288) follows the identical, now-three-times-verified safe pattern: every consumer scales off the constant, no hardcoded old value, no narrower-than-`uint32_t` type, and the `leafCount > cap` case is safe because hierarchy construction has zero dependency on the render cap. The new coherent-lane generation code — `SmoothStep`/`ValueNoise2D`/`Fbm2D`/`WrappedPhase`/`SpiralDustLaneStrength` — was traced function-by-function for the specific risk class the director named (NaN/Inf from log/sqrt/atan2/division on degenerate radius or angle wraparound): every division has either a fixed non-zero literal denominator or an explicit epsilon floor, `atan2` is used in the one place it's always safe (`sin`/`cos` of the same angle, never both zero), and the composed `SpiralDustLaneStrength` output is provably bounded in `[0,1]` for any finite input, including the `radius=0` and large-angle-wraparound edge cases checked explicitly. Determinism holds because every new function is pure and bottoms out in the codebase's existing documented-pure `random01`. One harmless redundant recomputation noted (not blocking). `coreEase` verified to land exactly in the director's stated `[0.72, 1.0]` range. Nebula pop constants are sane. Build is clean. Recommend merging as the new fidelity checkpoint.

---

# Validator Audit Report — coverage-overlap instrumentation + star footprint floor + energy conservation

**Methodology:** Correctness-only audit of the math and perf, per the director's explicit framing (aesthetic call already made from rendered output). Read `docs/galaxy_blur_coverage_analysis.md` first — the R&D doc that motivated this whole branch — to audit against the actual stated design intent, not just the diff in isolation. Reviewed in an isolated worktree at `/home/eric/Projects/pgu-cov-audit` (detached HEAD at branch tip).
**Branch reviewed:** `tune/coverage-overlap`, 3 commits: `9652543` (overlap-ratio telemetry), `977e296` (footprint floor, capped 1.8x), `14d4b0b` (energy conservation, cap raised to 4.0x). Full diff vs `origin/main` confirmed via `git diff origin/main...HEAD --stat`: 3 files — `galaxy_system.h` (+188/-6, nearly all of it), `main.cpp` (+2/-1, threading `exposure` into `BuildCut` + a window-title field), `tuning_panel.h` (+3, three new readouts). No shader files touched anywhere in this branch.
**Status:** **Clean on every explicitly-asked check. One substantive non-blocking finding surfaced below (§2) that the director didn't ask about but that's directly adjacent to the energy-conservation math this audit was scoped to verify.**

## 1. Energy conservation math — confirmed exact, verified two independent ways

Traced this at the level the director asked for — not "plausible," derived. `ApplyCoverageFootprintFloor` (`galaxy_system.h`) applies the identical scalar `bestScale` to all four axis components: `attr.axis1[0]`, `attr.axis1[1]`, `attr.axis2[0]`, `attr.axis2[1]` all multiplied by the same `s = bestScale` — this is **isotropic** scaling (same scalar on both principal axes, preserving their ratio/direction), not anisotropic elongation-preserving scaling. That's the precondition the director said would make `/s²` correct, and it holds.

**Verification 1 — CPU-side formula.** `EmitGaussian` (unchanged by this diff) sets `amplitude = kSplatAmplitudeScale · brightness / (2π·sqrtDet)` where `sqrtDet = sqrt(e1·e2)`, and `axis1`/`axis2` lengths are `3·sqrt(e1)`/`3·sqrt(e2)` — so `|axis1|·|axis2| = 9·sqrtDet`, i.e. `sqrtDet ∝ |axis1|·|axis2|`. Scaling both axes by `s` scales `|axis1|·|axis2|` (and thus `sqrtDet`) by exactly `s²`. To hold `amplitude·sqrtDet` (∝ integrated energy) constant, `amplitude` must divide by `s²` — exactly what `14d4b0b` does (`attr.amplitude /= bestScale * bestScale;`).

**Verification 2 — directly from the GPU shader**, independent of the CPU formula (in case the CPU-side "amplitude" derivation itself had a latent error that verification 1 wouldn't catch). Read `shaders/splat.wgsl`: the vertex shader offsets quad corners by `quad_corner.x·2·axis1 + quad_corner.y·2·axis2` — i.e., `axis1`/`axis2` are literally the screen-space basis vectors of the rendered ellipse — and the fragment shader outputs `radiance = color · amplitude · exp(-0.5·mahalanobis²) · exposure` in a `local_coord` space normalized so the ellipse is a fixed unit shape regardless of `axis1`/`axis2` magnitude. Since `axis1 ⊥ axis2` (they're the eigenbasis), mapping from that fixed unit shape to actual screen space has Jacobian `|axis1|·|axis2|`. So **integrated screen-space radiance = `amplitude · |axis1| · |axis2| · (fixed geometric constant)`** — confirmed directly from the rendering equation, not just the CPU-side amplitude formula. Scaling axes by `s` and dividing amplitude by `s²` leaves this product exactly unchanged. **Energy conservation is mathematically exact for the isotropic scaling actually used**, confirmed by two independent derivations that agree.

## 2. Substantive finding (not blocking, not asked about, but directly adjacent to §1): the floor undershoots its own stated visible-radius target once energy is conserved

This wasn't on the director's checklist, but it falls directly out of tracing the same energy-conservation math one step further, so flagging it rather than staying silent.

`bestScale` is computed *before* mutation, from `targetVisibleRadiusPx / currentVisibleRadiusPx`, where `VisibleRadiusPx` depends on `amplitude` through `peakHdr = amplitude·exposure·luminance` and `mahalanobisVisible = min(3, sqrt(2·log(peakHdr/floor)))`. After `14d4b0b`, `amplitude` is *also* divided by `s²` as part of applying that same `bestScale`. Since `mahalanobisVisible` is a function of `amplitude`, halving-by-`s²` isn't free with respect to the very metric used to size the floor: recomputing `mahalanobisVisible` with the new, smaller amplitude gives `mahalanobisVisible_after² = mahalanobisVisible_before² - 4·ln(s)` (algebra: `log(peakHdr/s²) = log(peakHdr) - 2log(s)`, doubled inside the sqrt). Since `s ≥ 1`, this is strictly ≤ the pre-conservation value whenever the mahalanobis term isn't already saturating at the `3.0` clamp on both sides.

Concretely, worked through the code's own formulas: a splat that needed the new `4.0×` cap and was sitting exactly at the `mahalanobisVisible = 3.0` ceiling *before* conservation ends up at `mahalanobisVisible ≈ 1.86` *after* dividing amplitude by `s²=16` (`3² - 4·ln(4) ≈ 9 - 5.545 = 3.455`, `sqrt ≈ 1.86`) — i.e., the splat's actual post-floor visible radius comes out to **~62% of `targetVisibleRadiusPx`**, not the full target, even though its raw 3σ geometric axis did grow by the full `4×`. The effect is smaller for splats needing only a modest scale-up (e.g. `s=2` from a saturated start: `9 - 4·ln(2) ≈ 6.23`, `sqrt≈2.5`, `mahalanobisVisible/3≈0.83` → ~83% of target) but never zero for any `s>1`.

**This does not contradict anything in §1** — total integrated energy is still exactly conserved; that's a separate, orthogonal quantity from "how far out does the brightness stay above the 1/255 display floor." Making a splat wider while holding its integral fixed necessarily dims it everywhere, including at the point that used to clear the visibility floor — so *some* undershoot of the visible-radius target is the unavoidable, physically-correct price of true energy conservation, not an implementation slip. What's worth flagging is that it's not accounted for anywhere in the `bestScale` computation itself (which is computed from the pre-conservation `VisibleRadiusPx`, then applied as if amplitude were unchanged) — so `kTargetCoverageOverlap = 1.05` is not actually the overlap ratio the floor converges to; the true converged ratio runs measurably below that, worse for exactly the sparsest/most under-covered splats the floor exists to help (the ones needing the largest `s`). Since `RecomputeCoverageTelemetryFromCut` re-measures post-floor state with the same formula, this undershoot is at least visible in the telemetry (§4) rather than silently hidden — but it's a distinct mechanism from the "summed tails" under-report the director already flagged as a known/documented caveat (that one is about missing cross-splat additive contributions; this one is a single-splat, purely-algebraic consequence of applying `bestScale` to amplitude with a stale, pre-mutation reference value). Given the director has already visually validated the rendered result as a clear win, and any real undershoot is still strictly an improvement over the pre-floor state (`s` is never < 1), I'm not calling this a blocker — but it's worth the director's awareness, particularly if `kTargetCoverageOverlap`/`kMaxCoverageFootprintScale` get retuned later against the telemetry number rather than the rendered image: the telemetry will read measurably below whatever ratio was actually being targeted.

## 3. Floor only enlarges — confirmed

`bestScale = std::clamp(targetVisibleRadiusPx / std::max(currentVisibleRadiusPx, 1.0e-4f), 1.0f, kMaxCoverageFootprintScale)` — the lower clamp bound is a literal `1.0f`, so `bestScale` can never be less than 1; there is no path that shrinks an already-adequately-covered splat. Upper-bounded at `kMaxCoverageFootprintScale = 4.0f`. The surrounding guard (`if (currentVisibleRadiusPx >= targetVisibleRadiusPx || baseEquivalentRadiusPx <= 0.0f) continue;`) additionally skips splats already at/above target entirely (no-op, not even a redundant `×1.0` multiply) and skips degenerate zero-radius splats. **No NaN/div0 possible:** the ratio's denominator is explicitly floored at `1.0e-4f` via `std::max`, and the whole floor pass is skipped up front if `estimatedScreenSpacingPx_ <= 0.0f` (guards the case where the spacing estimate itself is degenerate, e.g. a near-empty cut).

## 4. Still purely additive, no partition-of-unity — confirmed against the design doc's explicit guardrail

`docs/galaxy_blur_coverage_analysis.md` (the doc that motivated this branch) states explicitly: *"Don't add partition-of-unity normalization to smooth the field — that's opaque-surface-splatting semantics; the emissive star path must stay purely additive."* Verified: **zero `.wgsl` files are touched anywhere in this branch's diff** (confirmed via `git diff origin/main...HEAD --stat` — only `galaxy_system.h`/`main.cpp`/`tuning_panel.h`). The additive blend state (`splatAdditivePipeline_`, `srcFactor=One,dstFactor=One,Add`, already verified in an earlier audit) is completely untouched. This change only mutates CPU-side `PointStaticAttributes` (`axis1`/`axis2`/`amplitude`) before upload — no per-pixel weight, no shader-side normalization, no divide-by-accumulated-weight anywhere. **Dust and nebula paths confirmed untouched**: `git diff origin/main...HEAD -- galaxy_system.h | grep -E "^[+-]" | grep -i "dust\|nebula"` returns nothing — every line mentioning `dust`/`nebula` in the diff is unchanged context, not an added or removed line.

## 5. Perf — confirmed linear, not quadratic

Traced every new per-frame code path against `cutGaussianCount_` (up to `kMaxCutGaussians = 65536`):
* `AccumulateCoverageTelemetry` — called once per gaussian from `EmitGaussian` during `SelectCut`'s traversal, and again once per gaussian from `RecomputeCoverageTelemetryFromCut`'s loop. Each call is **O(1)**: a handful of arithmetic ops, no search over other gaussians.
* `ApplyCoverageFootprintFloor` — a single `for (i < cutGaussianCount_)` loop, O(1) work per gaussian (no nested loop, no lookup structure).
* The "spacing estimate" (`estimatedScreenSpacingPx_ = sqrt((width·height)/cutGaussianCount_)`) is **not** a nearest-neighbor search at all — it's a closed-form analytic proxy (bounding-box screen area of the whole cut, divided by count, square-rooted), computed once in `FinalizeCoverageTelemetry` from the four running min/max accumulators. This is exactly what the commit message calls it: a "screen-space spacing proxy," not literal per-splat nearest-neighbor distances — a deliberate O(1) approximation, not an accidentally-quadratic one.

Total added per-frame cost: **3 linear passes** over `cutGaussianCount_` (once during the existing `SelectCut` recursion — no new pass, since `AccumulateCoverageTelemetry` piggybacks on `EmitGaussian`'s call site — plus the floor pass plus the recompute pass), each strictly O(1) per gaussian. **No O(n²) anywhere.** At 65536 gaussians this is negligible next to the existing per-frame GPU upload/draw cost.

## 6. Instrumentation computes what it claims

Per `docs/galaxy_blur_coverage_analysis.md` §3, the target definition is *"mean visible footprint radius / mean nearest-neighbor screen spacing."* Verified the code computes this, with the same "visible vs. geometric" distinction the design doc calls "the crux": `HdrForDisplayFloor(1/255)` binary-searches the *inverse* of the existing `AcesFilmicScalar` tonemap to find the HDR input that tonemaps to exactly the 1/255 display floor, then each splat's `mahalanobisVisible = min(3, sqrt(2·log(peakHdr/floor)))` computes the Mahalanobis radius (in units of the 3σ half-extent, since `axis1`/`axis2` already encode 3σ) at which that specific splat's Gaussian crosses the visibility floor — a correct derivation of "where does `exp(-0.5·r²)` starting from `peakHdr` cross `floor`" (`r = sqrt(-2·ln(floor/peakHdr)) = sqrt(2·ln(peakHdr/floor))`, matching the code exactly). `coverageOverlapRatio_ = meanVisibleFootprintRadiusPx_ / estimatedScreenSpacingPx_` matches the ratio definition directly. The "spacing" side is honestly an *analytic proxy* rather than a literal nearest-neighbor computation (§5) — the design doc's own §3 uses "nearest-neighbor screen spacing" as the target concept, and the code's comment ("screen-space spacing proxy from projected cut bounds") is upfront that it's an approximation of that concept for O(1) cost, not literally computing it — consistent, not a discrepancy.

## 7. Build — clean

Fresh `cmake -S . -B build` + `cmake --build build -j32`: **exit 0**, no warnings, all targets linked.

## 8. Scope

Exactly the 3 files in the diff stat. No test files reference any of the new symbols (`CoverageOverlapRatio`, `ApplyCoverageFootprintFloor`, `kTargetCoverageOverlap`, `kMaxCoverageFootprintScale`, etc.) — nothing at risk of a stale assumption elsewhere.

## 9. Verdict

**Clean on every explicitly-asked check.** Energy conservation is mathematically exact — verified two independent ways (the CPU amplitude formula's own algebra, and directly from the GPU shader's radiance-integral Jacobian) — because the axis scaling is genuinely isotropic (same scalar on both principal axes), which is exactly the precondition the director named for `/s²` to be correct. The floor only enlarges (clamp floor of `1.0`, cap of `4.0`, explicit skip for already-adequate or degenerate splats), with no NaN/div0 path (all denominators floored). The change stays purely additive with zero shader files touched, confirmed against the design doc's explicit partition-of-unity guardrail, and dust/nebula are confirmed untouched by diff inspection. The per-frame coverage computation is linear (three O(n) passes, zero nearest-neighbor search — the spacing estimate is a deliberate analytic proxy). The instrumentation computes exactly what the motivating design doc specifies. Build is clean.

**One thing surfaced beyond the checklist (§2):** the energy-conserving amplitude division interacts with the same visible-radius metric used to size the floor, causing the floor to systematically undershoot its own `kTargetCoverageOverlap` for splats needing large scale-ups — not a bug (energy conservation itself is exact, and the mechanism is a physically-necessary consequence of true energy conservation, not an implementation slip), and not something that invalidates the director's already-confirmed visual result, but worth knowing if the telemetry numbers get used for future retuning rather than the rendered image. Recommend merging to `main`.

---

# Validator Audit Report — dust-lane turbulence refinement

**Methodology:** Correctness + scope audit, same rigor as the `SpiralDustLaneStrength` audit two passes back (director's own framing). Aesthetic call already made by the director from rendered output. Reviewed in an isolated worktree at `/home/eric/Projects/pgu-dustturb-audit` (detached HEAD at branch tip).
**Branch reviewed:** `tune/dust-turbulence`, commit `3dd5d2d` ("Add turbulent variation to coherent dust lanes"). Full diff vs `origin/main` confirmed via `git diff origin/main...HEAD --stat`: exactly 2 files — `galaxy_system.h` (+20/-6) and `shaders/dust_extinction.wgsl` (+6/-4). No other files touched.
**Status:** **Clean.**

## 1. Scope — confirmed, star-coverage and nebula paths genuinely untouched

`git diff origin/main...HEAD --name-only` returns exactly `galaxy_system.h` and `shaders/dust_extinction.wgsl` — no `renderer.h`, no `main.cpp`, no `tuning_panel.h` (the three files the just-merged coverage-overlap/footprint-floor branch touched). Went further than a filename check: `git diff origin/main...HEAD -- galaxy_system.h | grep -E "^[+-]" | grep -iE "nebula|coverage|footprint|ApplyCoverageFootprintFloor|EmitGaussian|VisibleRadiusPx"` returns **nothing** — no added or removed line anywhere in this diff even mentions the star coverage-floor machinery or the nebula path by name. `find shaders -name "*.wgsl"` lists 4 shader files (`dust_extinction.wgsl`, `hardware_point.wgsl`, `postprocess.wgsl`, `splat.wgsl`); the diff touches only `dust_extinction.wgsl` — `splat.wgsl` (the star/coverage-floor consumer) and `postprocess.wgsl` (bloom/tonemap, also nebula's HDR feed) are untouched. The recently-merged smooth-haze fix is not perturbed by this branch.

## 2. New turbulence generation math — traced for NaN/Inf/div0/degenerate, same rigor as the prior `SpiralDustLaneStrength` audit

`SpiralDustLaneStrength` gained 6 new `Fbm2D` calls (`laneCoarse`, `laneFine`, the `laneWidth` noise, `branchNoise`, the `branchGate` input, `gapNoise`, `edgeFray`) plus a 7th in `GenerateDustSamples` (`laneTurbulence`). Every one of them is a call to the already-audited `Fbm2D`, which was previously proven to always return a finite value in `[0,1)` for any finite input (convex combination of bounded `ValueNoise2D`/`random01` outputs, fixed positive normalizer) — that guarantee carries over unchanged since `Fbm2D` itself isn't modified by this diff, only its call sites/arguments are new.

**The one new risk surface worth independent verification: two of these noise outputs now feed a *denominator*, where the old code used a fixed literal.** Traced both:
* `laneWidth = 0.20f + 0.26f * Fbm2D(...)`. Since `Fbm2D ∈ [0,1)`, `laneWidth ∈ [0.20, 0.46)` — **provably always ≥ 0.20, never zero or negative.** `laneDistance / laneWidth` is therefore always safe.
* `branchWidth = laneWidth * (0.38f + 0.22f * branchNoise)`, with `branchNoise ∈ [0,1)` so the second factor is `∈ [0.38, 0.60)`. Product of two strictly-positive bounded factors (`laneWidth ≥ 0.20`, multiplier `≥ 0.38`) is itself strictly positive (`≥ 0.076`), so `branchDistance / branchWidth` is also always safe.

Both new masks (`laneMask`, `branchMask`) are `exp(-0.5·ratio²)` with a provably-positive-bounded `ratio` — bounded in `(0, 1]`, never `Inf`/NaN, same reasoning already verified for the pre-existing lane mask. `branchGate` and `continuity` are both `SmoothStep(...)` outputs (already proven clamp-bounded `[0,1]` for any finite input, epsilon-floored denominator, unconditionally safe regardless of what noise feeds it) linearly rescaled — `continuity ∈ [0.25, 1.30]`, always positive. `edgeFray = 0.62 + 0.68·Fbm2D(...) ∈ [0.62, 1.30)`, always positive. `structuredMask = max(laneMask, branchMask·branchGate·0.58)` — max of two non-negative bounded quantities, itself bounded `(0, 1]`. Final `clamp(structuredMask · continuity · edgeFray, 0, 1)` — product of three bounded positive factors, clamp is redundant-but-harmless. **No path through this function can produce NaN, Inf, or a negative value.**

`laneTurbulence = 0.42f + 1.30f * Fbm2D(...) ∈ [0.42, 1.72)`, always strictly positive — multiplied into `opticalDepth` alongside the pre-existing `coreEase ∈ [0.72, 1.0]` and the other already-positive factors (traced in the prior dust-lanes audit), so `opticalDepth` remains strictly positive, unchanged conclusion.

**Dust sample validity (positive covariance, finite positions):** confirmed by diff inspection that **none of the new code touches position (`u`,`v`,`w`), the covariance construction (`alongArm`/`acrossArm`/`longSigma`/`shortSigma`/`verticalSigma`/`CovarianceFromAxes`), or `effRadius`** — every changed/added line is either inside `SpiralDustLaneStrength` (whose output only ever feeds the pre-existing, unchanged `acceptProb` rejection-sampling gate and the `opticalDepth` weight) or the `opticalDepth` line itself (a brightness/depth scalar, not a geometric term). Since geometry construction is untouched and `laneStrength`/`opticalDepth` were already proven bounded/positive, dust sample validity carries over unchanged from the prior audit — nothing new to break here structurally.

**Stream-ID collision check:** listed all 8 new `Fbm2D` calls' `(thetaPitch multiplier+offset, radius multiplier, stream)` triples — all 8 are distinct (no two calls share the same coordinate-scaling-and-stream combination), so each modulation samples a genuinely independent noise field rather than accidentally moving in lockstep with another term (which would silently defeat the "turbulent" visual intent, though not cause a crash). `laneCoarse` reuses stream `91` from the pre-existing (now-removed) `laneNoise` term, but with different coordinate multipliers (`0.34`/`0.46` vs. the old `0.38`/`0.52`) — consistent with the established codebase pattern of decorrelating via coordinate scaling, not stream-number uniqueness alone; not a bug.

**Determinism:** every new term is a call to `Fbm2D(seed, ...)`, itself proven pure (bottoms out in the documented-pure `random01`) in the prior audit — no new source of non-determinism introduced (no new mutable state, no incidental iteration-order dependence). Same `seed` reproduces identical lanes/turbulence, unconditionally.

## 3. Dust cap/leaf-count constants — none changed

`git diff origin/main...HEAD -- galaxy_system.h | grep -E "^[+-]" | grep -iE "kMaxDustGaussians|kDustLeafCount|kMaxNebulaGaussians|kNebulaLeafCount|kMaxCutGaussians"` returns nothing — this branch touches no buffer-sizing constants at all, so the buffer-sizing consistency check from the prior three passes doesn't apply here. Noting per the director's instruction rather than skipping silently.

## 4. Coherent arm-space structure remains dominant — confirmed structurally, not just aesthetically

Checked the specific thing that would distinguish "turbulence as modulation" from "reversion to independent per-blob clumps": **whether the new noise terms are functions of the sample's position in arm-space (`thetaPitch`, `radius` — shared/coherent across nearby samples) or functions of the per-sample loop index `i` (which would make them independent per star, i.e. a clump).** Every one of the 7 new `Fbm2D` calls in `SpiralDustLaneStrength` and `GenerateDustSamples` takes `(thetaPitch·k₁+c₁, radius·k₂, stream)` as its coordinate arguments — **none of them take the loop index `i`.** Since `Fbm2D`/`ValueNoise2D` are continuous, coherent functions of their `(x,y)` input (that's the entire point of value-noise-based fBM — nearby inputs give correlated outputs), two dust samples at similar `(thetaPitch, radius)` see nearly the same noise field values, preserving spatial coherence across samples — this is architecturally the same "arm-space field" pattern as the original (pre-turbulence) `SpiralDustLaneStrength`, just with more terms composited into it.

Also checked the specific compositing operator: `structuredMask = std::max(laneMask, branchMask·branchGate·0.58)`. Since the branch term is capped at `≤ 0.58` while `laneMask` reaches up to `1.0`, **the primary lane term dominates completely everywhere `laneMask` alone already exceeds `0.58`** (i.e., anywhere reasonably close to the lane centerline) — the branch term can only ever matter in the outer/gap regions where the primary lane mask has already fallen off. This confirms by construction, not just by reading the commit message, that branches are additive "wisps" layered on top of a still-dominant coherent lane, not a replacement for it.

## 5. Shader-side changes — confirmed within-primitive texture only, no new div-by-zero

`shaders/dust_extinction.wgsl`'s `fs_main` gained one new noise term (`edge_noise`, frequency `16.0`) and a `fray` factor, plus retuned coefficients on the existing `ridge_noise`/`fine_noise`/`filament` terms. All of these operate purely on `input.local_coord` (the intra-primitive fragment coordinate, `|local_coord|≤1` inside the 3σ disk) — they never touch `input.optical_depth`/`input.extinction`/`input.center`, which are the CPU-assigned per-primitive values verified untouched in §2. `fbm()`'s normalizer is frequency-independent (proven in an earlier audit), so the new `16.0` frequency for `edge_noise` is exactly as safe as the existing `9.5`/`34.0` frequencies. `smoothstep(0.38,0.80,...)` and `smoothstep(0.30,0.82,...)` both have valid `edge0<edge1` ordering. `fray = 0.78 + 0.44·smoothstep(...) ∈ [0.78, 1.22]`, always positive; `filament` and the final `(0.28+1.85·filament)` term are sums of non-negative products, always non-negative; `optical_depth`'s new `· fray` factor is a strictly-positive multiplier, introducing no new division anywhere. `transmittance = exp(-extinction·optical_depth)` remains division-free.

## 6. Build — clean

Fresh `cmake -S . -B build` + `cmake --build build -j32`: **exit 0**, no warnings, all targets linked.

## 7. Scope (test files)

No test file references `SpiralDustLaneStrength`, `laneOffset`, `laneWidth`, or `branchNoise` — nothing at risk of a stale assumption.

## 8. Verdict

**Clean.** Scope is exactly as claimed: only `galaxy_system.h` and `shaders/dust_extinction.wgsl` changed, with the star-coverage/footprint-floor machinery and the nebula path confirmed untouched by direct diff-content inspection (not just a filename check), and `splat.wgsl`/`postprocess.wgsl` untouched. The new turbulence math was traced with the same rigor as the prior `SpiralDustLaneStrength` audit: every new noise term is a proven-bounded `Fbm2D` call, the two new noise-driven denominators (`laneWidth`, `branchWidth`) are both provably bounded strictly above zero, and the composed output stays clamp-bounded `[0,1]` for any finite input — no NaN/Inf/div0 possible. Dust sample geometry (position, covariance) is untouched by this diff, so validity carries over from the prior audit unchanged. Every new noise term is deterministic (pure `Fbm2D`/`random01` chain, no new mutable state), and all 8 new calls sample independent noise fields (no accidental stream/coordinate duplication). The coherent arm-space structure is confirmed still dominant both by construction (all new terms are functions of position in arm-space, not per-sample index) and by the specific `max()` compositing (branches capped at `0.58`, can't override the primary lane near its centerline). No dust/nebula/star cap constants changed. The shader-side change is confirmed to be purely within-primitive texture, introducing no new division. Build is clean. Recommend merging to `main`.

---

# Validator Audit Report — star-color pass (stellar catalog, blackbody, blue-arms decouple)

**Methodology:** Correctness-only audit of a 4-commit stack (aesthetic already signed off by the director from rendered output). Read both motivating design docs in full before touching code — `docs/galaxy_blue_arms_analysis.md` (the structural root-cause diagnosis that specified the exact fix) and `docs/stellar_population_framework.md` (the catalog/sampling/blackbody spec) — then audited the diff against both, not just for internal consistency. Reviewed in an isolated worktree at `/home/eric/Projects/pgu-color-audit` (detached HEAD at branch tip).
**Branch reviewed:** `tune/star-color` at `e6941ee`, 4 commits: `663ac8b` (stellar catalog + blackbody + per-star population sampling), `161c88b` (hue-preserving tonemap), `4159c0f` (luminosity compression for render brightness), `e6941ee` (the key fix — decouple color weight from brightness weight, plus blue-population saturation polish). Full diff vs `origin/main`: `galaxy_model.h` (+150/-32), `galaxy_system.h` (+25/-19... net small since most of this is the 4-way `GaussianNode` initializer-list plumbing described below), `shaders/postprocess.wgsl` (+13/-2). No other files touched.
**Status:** **Clean.**

## 1. THE DECOUPLE (most important check) — confirmed byte-equivalent amplitude path

This got the most scrutiny, per the director's framing. Read the full star-merge loop (`galaxy_system.h`, `BuildStarHierarchyFromSamples`) before and after `e6941ee` side by side (`git diff 4159c0f e6941ee -- galaxy_system.h`) rather than trusting the commit message.

**Color weighting, confirmed correct:** the merge loop's color accumulation changed from `weightedColor += n.brightness * n.color` to `weightedColor += n.colorWeight * n.color`, and the final division changed from `weightedColor * invBrightness` to `weightedColor * invColorWeight` where `invColorWeight = 1/colorWeightSum` (falling back to `invBrightness` only in the unreachable — see below — `colorWeightSum == 0` case). `colorWeightSum` is accumulated as `Σ child.colorWeight`, exactly matching the director's stated formula `node.colorWeight = Σ(child.colorWeight)`. At the leaf level, `colorWeight = std::max(luminosity, 1.0e-4f)` — the **raw, near-uncompressed** sampled luminosity (not `renderLuminosity`, the compressed one) — exactly the doc's prescribed fix: *"each StarSample carries a colorWeight (∝ real luminosity)... separately from brightness (∝ renderLuminosity, unchanged)."*

**Amplitude/energy path, confirmed byte-equivalent — this is the property that actually matters for not breaking the coverage floor.** Diffing the merge loop line-by-line: `brightnessSum += n.brightness;`, the entire `weightedPos` accumulation (still weighted by `n.brightness`, untouched), `invBrightness = brightnessSum > 0.0f ? 1.0f/brightnessSum : 0.0f;`, and the covariance loop (`const float w = n.brightness * invBrightness;`, unchanged) — **none of these lines appear in the diff at all** (confirmed via `git diff 4159c0f e6941ee -- galaxy_system.h`, they're pure unchanged context). `node.brightness` is still `brightnessSum`, computed by the identical formula over the identical input (`n.brightness`) as before this commit. At the leaf level, `StarSample.brightness` is still `renderLuminosity * kLuminosityBrightnessScale` — the exact same expression, unchanged by `e6941ee` (verified the leaf-construction line in `ApplyStellarPopulationColor` — only `color` and the new `colorWeight` field were added to that function; `brightness`'s formula is untouched from `4159c0f`). Went one step further than the director's ask: grepped the **entire branch diff** for `EmitGaussian`, `amplitude`, `ApplyCoverageFootprintFloor`, `VisibleRadiusPx`, `kSplatAmplitudeScale` — **zero hits anywhere in this branch**. The whole downstream chain (hierarchy `brightness` → `EmitGaussian`'s amplitude derivation → the coverage floor from the previous audit) is not merely "equivalent," it's **untouched by a single line** in this branch. The smooth-haze/coverage-floor guarantee from two audits ago cannot have been perturbed by this change — there's no code path left for it to have been perturbed through.

**One structural note, not a bug:** `GaussianNode` gained the `colorWeight` field as its 4th member, which forced every `GaussianNode{...}` positional-initializer call site across dust, nebula, *and* star code to grow by one argument (the struct is shared across all three gaussian kinds) — that's why `galaxy_system.h`'s diff touches dust/nebula construction lines despite this being a star-only feature. Checked whether dust/nebula's *own* color merging was affected: grepped for `colorWeight` — it appears **only** inside the star merge loop (lines populating/reading `colorWeightSum`); dust and nebula's leaf/merge `GaussianNode{...}` calls pass their own `brightness`/`opticalDepth` value twice (once for `brightness`, once for the now-mandatory `colorWeight` slot — e.g. `GaussianNode{d.localPos, d.extinction, d.opticalDepth, d.opticalDepth, ...}`), and their internal `weightedExtinction`/`weightedColor` accumulation loops (unchanged by this diff) still weight by `n.brightness`/`n.opticalDepth` exclusively — `colorWeight` is populated but never read for dust/nebula. **Dust and nebula color merging is functionally byte-identical to before**, confirmed, not just assumed from the diff stat.

## 2. No NaN/div0 — confirmed across every new code path

* **`colorWeight` zero-sum guard:** every catalog entry's `luminosityMin > 0` (smallest is the M-dwarf row's `0.0001`), and `colorWeight = max(luminosity, 1.0e-4f)` floors it further — so every individual star's `colorWeight` is provably `≥ 1.0e-4f > 0`. A sum of one-or-more such positive terms (`groupCount ≥ 1` always, per the merge fanout logic) can never be exactly zero — so `colorWeightSum > 0.0f` is **always true in practice**; the `: invBrightness` fallback is defensive, not a reachable code path, and even if it somehow were, falling back to the (also-safe) brightness-based weight is a sane degenerate choice, not a crash/NaN.
* **`BlackbodyToRGB`:** clamps `temperatureKelvin` to `[1000, 40000]` **before** any branch, so every subsequent `log`/`pow` operates on a safely-bounded domain — traced each branch: the cool-branch `log(t/100)` has `t/100 ≥ 10 > 0`; the hot-branch `pow(t/100-60, ...)` has `t/100-60 ≥ 6 > 0` (since this branch only runs for `t > 6600`); the blue cool-branch `log(t/100-10)` has `t/100-10` ranging `(9, 56)`, always positive; the hot-branch `pow(t, -1.5)` has `t ≥ 1000 > 0`. **No domain violation possible anywhere in this function**, regardless of what `temperatureKelvin` is passed in.
* **Luminosity compression (`4159c0f`):** `pow(max(luminosity, 1.0e-4f), 0.42f)` — base floored positive before the fractional-exponent `pow`, safe; `min(..., 16.0f)` bounds the result. No NaN/Inf possible.
* **`SaturateHotStarColor`:** no division anywhere — pure multiply/add/clamp. `hotBlend` is a `StellarSmoothStep` output (already proven clamp-bounded `[0,1]` for any finite input in an earlier audit), `saturation = 1 + 2·hotBlend ∈ [1,3]`, each output channel individually clamped to `[0,1]`. Unconditionally safe.
* **Hue-preserving tonemap (`shaders/postprocess.wgsl`):** `scale = aces_filmic_scalar(luminance) / max(luminance, 1.0e-4)` — denominator explicitly floored, safe at `luminance = 0` (`scale = 0/1e-4 = 0`, correctly maps black to black, not NaN). `aces_filmic_scalar`'s own internal division (`.../(x·(c·x+d)+e)`) has `e = 0.14` as a fixed positive additive constant and `hdr` is pre-floored to `≥ 0` via `max(scene+bloom·bloom_strength, vec3(0))` before luminance is computed, so `x ≥ 0` always in this call, making the denominator `≥ e = 0.14 > 0` unconditionally — no div0 possible even before the outer `max(luminance, 1e-4)` guard. **Hue preservation confirmed by construction, not just by testing:** the same scalar `scale` multiplies all three channels uniformly (`hdr * scale`), which by definition preserves the ratio between channels (hue and relative saturation) exactly — this is the mathematical definition of what "hue-preserving" means here, not an approximation of it.

## 3. Blackbody formula + catalog sampling vs. `docs/stellar_population_framework.md`

**Sampling procedure — exact match.** The doc's step 2 is explicit: *"Draw one random fraction f ∈ [0,1] and use it to interpolate both temperature and luminosity within that entry's ranges together... not two independent draws."* Code: `const float f = random01(seed, starIndex, 17); temperature = lerp(Tmin,Tmax,f); luminosity = lerp(Lmin,Lmax,f);` — the same `f` feeds both interpolations, exactly as specified, preserving the temperature-luminosity correlation the doc calls out as the point of this specific requirement.

**Weighted catalog pick — matches the doc's population-subtable design.** `PickStellarCatalogEntry` computes `totalWeight` from `entry.oldWeight·(1-youngBlend) + entry.youngWeight·youngBlend` per entry, then does a standard cumulative-weight random pick — this is the doc's "pick a catalog entry by weighted random choice" with the Old/Young blend the doc's Disk-component section calls for. Checked `StellarPopulationYoungWeight`'s per-`ComponentKind` mapping against the doc's population sub-table section: `Bulge`/`DiffuseHalo → 0.0` (doc: *"Old sub-table only... O/B/A weights zeroed"*, matches — and indeed the catalog's `oldWeight` column has A/B/O all literally `0.0f`); `Disk → StellarSmoothStep(0.42, 0.92, normalizedArmDensity)` (doc: blend via the existing arm-density signal, matches structurally, and the specific `0.42`/`0.92` thresholds match the values already used elsewhere in this codebase's arm-density blending, e.g. the dust turbulence audit two passes back used the same `0.42` constant in an analogous role); `RingAnnulus`/`Clumpy → 1.0` (doc: *"Young, strongly biased"*, matches as the fully-young extreme); `Bar → 0.18` (doc: *"Old/Mixed... not zero young population, but not boosted"*, matches directionally — mostly old with a modest young fraction).

**Blackbody formula — matches the doc's cited formula on R exactly; G/B use the fuller canonical coefficients rather than the doc's abbreviated single-line summary (worth noting, not a mismatch).** The doc gives an explicit simplified formula (`R ≈ 56100000·T^(−3/2)+148`, `G ≈ 100.04·ln(T)−623.6`, `B ≈ 194.18·ln(T)−1448.6`) citing it as "a standard blackbody-temperature→RGB polynomial fit... popularized as a simple approximation." The code's hot-branch R term (`56100000·t^{-1.5}+148`) matches the doc's R formula **literally, coefficient-for-coefficient**. The code's G/B terms instead use the well-known fuller piecewise-polynomial coefficients from the same standard reference family (`99.4708025861`, `161.1195681661`, `288.1221695283`, `0.0755148492`, `138.5177312231`, `305.0447927307` — recognizable as the canonical Tanner Helland/Mitchell-Charity blackbody approximation's published constants) rather than the doc's own rounded one-line summary for G/B. Algebraically checked these aren't inconsistent: expanding the doc's cool-branch G citation in terms of `ln(T/100)` (`99.47·ln(T/100) - 161.12` ⟺ `99.47·ln(T) - 619.1`) lands close to the doc's stated `100.04·ln(T) - 623.6` — small rounding-level differences consistent with the doc citing an illustrative simplified form of the same source rather than a literal spec to transcribe. Net: this is a correct implementation of "the standard blackbody approximation" the doc calls for, using the more complete/accurate coefficients from that same standard family for G/B than the doc's own abbreviated inline citation — not a deviation from a different, unrelated source.

**Catalog table numbers — match the doc's stated values where the doc gives them, and use the doc's own "tunable" latitude where it doesn't.** Old-table weights (M 61, K 12, G 7.5, F 3.0) match the doc's numbers exactly. The doc doesn't give literal numeric IMF-derived young weights (it says *"use the stellar initial mass function... Kroupa's field IMF is a broken power law"* without spelling out a closed-form integral to compute per-type weights from) — the code's young-weight column (M 61, K 18, G 9, F 5.5, A 3.5, B 0.22, O 0.006) is a hand-tuned approximation of the IMF's shape (much higher A/B/O representation than the old table's exact zero, while still keeping O vanishingly rare) rather than a literal Kroupa-integral derivation. The doc explicitly frames these weights as "tunable, not gospel," so this is within the doc's own stated latitude, not a spec violation — flagging only so the director knows the young weights are a shape-matching approximation, not a literal IMF-integral computation.

## 4. RADIUS TRAP — confirmed clean, and structurally impossible to violate

Checked the strongest possible version of this: `StellarCatalogEntry` (`galaxy_model.h`) has exactly 6 fields — `temperatureMin/Max`, `luminosityMin/Max`, `oldWeight`, `youngWeight` — **there is no radius field in the catalog at all**, so there's no physical-radius value even present to accidentally wire into rendering. `gaussianRadius` is computed by the same per-`ComponentKind` formula as before this branch (confirmed unchanged — the `switch(comp.kind)` block that sets `gaussianRadius` in `GenerateStar` had only its `brightness = ...` lines removed in `663ac8b`; every `gaussianRadius = ...` line is untouched), then threaded through as a plain parameter into `ApplyStellarPopulationColor`/`StarSample` without ever touching the catalog. Grepped the full branch diff for every line touching `gaussianRadius`/`.radius` — confirmed all of them are pure pass-through plumbing (parameter passing, struct field assignment), none derive a new value from stellar type. Render footprint size is provably independent of stellar type in this implementation.

## 5. Blue-population saturation polish — gated correctly, sane bounds

`SaturateHotStarColor` early-returns the input color unchanged whenever `hotBlend ≤ 0` (i.e., `temperatureKelvin ≤ 7500`, via the `StellarSmoothStep(7500, 22000, ...)` gate) — **confirmed not global**: cool/warm stars (the vast majority of the population, per §3's weight tables) take this early-return path and are completely unaffected. `saturation = 1 + 2·hotBlend` is bounded `[1.0, 3.0]` exactly matching "max 3x." Called exactly once, at leaf-star generation only (grepped: 2 occurrences in `galaxy_model.h` — 1 definition, 1 call site, 0 in `galaxy_system.h`) — **not re-applied at merge time**, so hierarchy levels don't compound the saturation boost. No division anywhere in the function; every output channel individually clamped to `[0,1]` — no over-saturation NaN possible.

## 6. Build — clean

Fresh `cmake -S . -B build` + `cmake --build build -j32`: **exit 0**, no warnings, all targets linked.

## 7. Scope (tests)

No test file references `StarSample`, `GaussianNode`, `colorWeight`, `BlackbodyToRGB`, or `kStellarCatalog` — nothing at risk of a stale assumption.

## 8. Verdict

**Clean.** The decouple — the property flagged as most important — is confirmed correct at the level the director asked for: `node.colorWeight = Σ(child.colorWeight)` with leaf `colorWeight = max(realLuminosity, 1e-4)`, node color computed as `Σ(colorWeight_i·color_i)/Σ(colorWeight_i)`, while the entire amplitude/energy path (`brightnessSum`, position weighting, covariance weighting, `node.brightness`, and everything downstream through `EmitGaussian`/the coverage floor) is untouched by even a single line in this branch — not merely equivalent in effect, literally absent from the diff. No NaN/div0 anywhere: every new denominator (`colorWeightSum`, the tonemap's luminance divisor, `aces_filmic_scalar`'s internal denominator) is either provably non-zero by construction or explicitly epsilon-floored, and `BlackbodyToRGB`'s temperature clamp makes every subsequent `log`/`pow` domain-safe unconditionally. The Radius Trap is structurally avoided — the catalog carries no radius field at all, and `gaussianRadius` is confirmed to be pure unchanged pass-through plumbing. The blackbody formula matches the doc's own cited R formula literally and uses the fuller canonical coefficients (from the same standard reference family) for G/B; catalog weights match the doc's stated numbers where given and use the doc's own explicit "tunable" latitude for the non-doc-specified IMF-shaped young weights. The blue-saturation polish is correctly hot-star-gated (not global), bounded to the stated max 3x, and applied once at leaf generation only. Build is clean. Ready to merge pending Eric's sign-off on the blue saturation level, as the director noted.

---

# Validator Audit Report — vivid blue-arms delta (on top of the audited color pass)

**Methodology:** Correctness-only audit of the delta since the previously-fully-audited `e6941ee` (aesthetic already signed off by the director). Same critical property as the prior color-pass audit: confirm the amplitude/energy path is still untouched, then verify the new color-grade math specifically. Reviewed in an isolated worktree at `/home/eric/Projects/pgu-vivid-audit` (detached HEAD at branch tip).
**Branch reviewed:** `tune/star-color` at `61857b7` ("Enhance vivid blue arm color"), one commit on top of the audited `e6941ee` (plus my own prior audit-doc commit `f5fb9bf` in between, docs-only). `git diff e6941ee...61857b7 --name-only`: `docs/validator_audit.md` (my own prior report) and `galaxy_model.h` (+30/-2) — no other file touched.
**Status:** **Clean.**

## 1. STILL COLOR-ONLY — confirmed, amplitude/energy path has zero touch

Same check as last time, re-run against the new delta specifically: `git diff e6941ee...61857b7 -- galaxy_model.h galaxy_system.h shaders/ | grep -E "^[+-]" | grep -iE "brightness|renderLuminosity|kSplatAmplitudeScale|gaussianRadius|EmitGaussian|ApplyCoverageFootprintFloor|VisibleRadiusPx|kLuminosityCompressionPower|kMaxCompressedLuminosity"` returns **nothing**. `galaxy_system.h` and every shader file are entirely absent from this delta's changed-files list — the merge/hierarchy code (where `brightnessSum`/`colorWeightSum`/`invBrightness` live, already verified byte-equivalent in the prior audit) isn't touched at all this time, by construction, since this delta doesn't modify `galaxy_system.h`. Within `galaxy_model.h` itself, the only touched lines are: `SaturateHotStarColor`'s saturation constant (`2.0f→4.5f`), the new `GradeYoungArmColor` function, and one line in `ApplyStellarPopulationColor` where `color`'s expression grew a `GradeYoungArmColor(...)` wrapper and one new local (`youngBlend`, computed via the pre-existing, unchanged `StellarPopulationYoungWeight`). The `StarSample{...}` return line's `brightness`/`colorWeight`/`radius`/`localPos` arguments are unchanged context — confirmed by reading the diff directly, not inferred. This delta is provably color-only.

## 2. Young-arm blue grade math — traced for NaN/div0, domain-edge behavior, and boundedness

`GradeYoungArmColor(color, youngBlend, temperatureKelvin)`:
* **Gating, confirmed young-arm-only:** `if (youngBlend <= 0.0f) return color;` is the first line — any star whose component/arm-density combination yields `youngBlend = 0` never enters the grade at all. `youngBlend = StellarPopulationYoungWeight(comp.kind, normalizedArmDensity)` (unchanged, pre-existing function) returns a **hardcoded literal `0.0f` for `Bulge`/`DiffuseHalo`** — meaning bulge stars structurally cannot be graded, full stop, independent of temperature or anything else. This is the strongest possible confirmation of "core stays warm" for the arm-blue grade specifically: it's not a tuning choice that happens to leave the core alone, the gate makes it impossible for the core to be touched by this function. For `Disk`, `youngBlend = StellarSmoothStep(0.42, 0.92, normalizedArmDensity)` is also exactly `0` for any star at or below `0.42` normalized arm density (inter-arm/low-density disk regions) — those stars are equally ungraded.
* **`hotBlend = StellarSmoothStep(6200, 17000, temperatureKelvin)`** — reuses the already-proven-bounded `[0,1]`, domain-safe-for-any-finite-input `StellarSmoothStep` (epsilon-floored denominator, verified in the base color-pass audit); the specific edge values (`6200`/`17000` here vs. `7500`/`22000` for the saturation gate) don't change that safety property, since it doesn't depend on the particular edge constants. At `T ≤ 6200`: `hotBlend = 0`. At `T ≥ 17000`: `hotBlend = 1`. Continuous (Hermite smoothstep) in between, no discontinuity at either edge.
* **`armBlue = clamp(youngBlend·(0.86+0.44·hotBlend), 0, 1)`** — explicitly clamped, so even though the raw product could exceed `1` (e.g. `youngBlend=1, hotBlend=1 → 1.30`), the code catches it directly rather than relying on the inputs happening to stay in range. **Confirmed bounded `[0,1]` by an explicit guard, not just by assumption.**
* **The three `graded.{x,y,z}` channels are each a true convex combination** (verified algebraically, not just visually): `graded.x = color.x·(1-0.82·armBlue) + target.x·(0.82·armBlue)` — weights `(1-0.82·armBlue)` and `(0.82·armBlue)` are both non-negative for `armBlue∈[0,1]` (ranging `[0.18,1.0]` and `[0,0.82]` respectively) and sum to exactly `1` — a textbook non-negative-weighted average of `color.x` and `target.x`, both already in `[0,1]`, so `graded.x` is bounded in `[0,1]` **by construction**, independent of the outer `clamp`. Same reasoning for `graded.y` (weights `[0.34,1.0]`/`[0,0.66]`). `graded.z = color.z + 0.55·armBlue·(1-color.z)` algebraically expands to `color.z·(1-0.55·armBlue) + 1.0·(0.55·armBlue)` — the identical convex-combination pattern toward `target.z=1.0`, just written in the equivalent "push toward the ceiling" form since `target.z` happens to be exactly `1.0`. All three channels: bounded by construction, the `clamp` calls are a correct-but-redundant extra safety net, not the only thing preventing overflow.
* **The luminance-preserving scale — this is the "+10% arm glow... actually bounded" check.** `scale = clamp((sourceLum·(1+0.10·armBlue)) / max(gradedLum, 1.0e-4), 0.25, 1.8)`. Division guarded by the `1.0e-4` floor (never zero); `gradedLum` in practice is comfortably above that floor whenever `armBlue>0` since the fixed `target` color's own luminance is `≈0.509` (`0.24·0.2126+0.54·0.7152+1.0·0.0722`), nonzero, so the floor is a defensive backstop rather than a commonly-hit case. **`scale` is explicitly clamped to `[0.25, 1.8]`** — confirmed this is a real, load-bearing bound, not decorative: the raw ratio `sourceLum·(1.10)/gradedLum` has no inherent bound on its own (if `gradedLum` were small relative to `sourceLum`, the unclamped ratio could be arbitrarily large), so the explicit `1.8` ceiling is what actually prevents an unbounded brightness multiplier here, and the `0.25` floor prevents an unbounded darkening. Directly answers the "actually bounded, not unbounded" ask.
* **Final output**: `clamp(graded.{x,y,z} · scale, 0, 1)` per channel — this final clamp **is** load-bearing (unlike the `graded` computation's redundant one): since `scale` can reach `1.8` and `graded` components can individually reach `1.0`, the pre-clamp product can reach `1.8`, so this clamp is what actually keeps the final output in `[0,1]`. Confirmed present and applied to all three channels.

**No NaN/Inf anywhere in this function** — every operation is multiply/add/a single guarded division/smoothstep/clamp; no `log`/`pow`/`sqrt` at all in `GradeYoungArmColor` itself (unlike `BlackbodyToRGB`, which isn't touched by this delta).

## 3. Colors stay in `[0,1]` after grade + 5.5× saturation

Traced the full leaf-color pipeline as composed in `ApplyStellarPopulationColor`: `BlackbodyToRGB` (clamps to `[0,1]` internally, unchanged by this delta) → `SaturateHotStarColor` (new constant: `saturation = 1+4.5·hotBlend ∈ [1.0, 5.5]`, exactly matching the described "5.5x" cap; every output channel individually `clamp`-ed to `[0,1]`, unchanged logic from the prior audit, just a bigger constant) → `GradeYoungArmColor` (per §2, every intermediate and the final output explicitly clamped to `[0,1]`). Each stage independently guarantees `[0,1]` output regardless of its input's exact value (as long as the input itself is finite, which it is at every stage) — so the **composition** is guaranteed `[0,1]` by the same chain-of-clamps reasoning, not just checked for this specific pair of constants. No negative or overflowing channel values possible.

## 4. Build — clean

Fresh `cmake -S . -B build` + `cmake --build build -j32`: **exit 0**, no warnings, all targets linked.

## 5. Scope (tests)

No test file references `GradeYoungArmColor` or `SaturateHotStarColor` — nothing at risk of a stale assumption. `GradeYoungArmColor` and `SaturateHotStarColor` are each called exactly once, chained together in `ApplyStellarPopulationColor` (leaf-star generation only) — grepped both names across `galaxy_model.h`/`galaxy_system.h`: zero occurrences in the merge code, confirming (as with the prior audit's saturation function) that this grade is **not re-applied at merge time** — no compounding across hierarchy levels.

## 6. Verdict

**Clean.** The vivid delta is provably color-only: `galaxy_system.h` and every shader file are completely absent from this delta's file list, and within `galaxy_model.h` the only touched lines are the saturation constant, the new grade function, and one call-site wiring — the amplitude/energy path (`brightnessSum`, `EmitGaussian`, the coverage floor) has zero touch, same critical property confirmed clean as the base color-pass audit. `GradeYoungArmColor` is correctly young-arm-gated: `Bulge`/`DiffuseHalo` have `youngBlend` hardcoded to exactly `0`, so the core is *structurally* incapable of being graded by this function, not merely untouched by choice of parameters. `armBlue` and the luminance-preserving `scale` are both explicitly, load-bearingly clamped (`[0,1]` and `[0.25,1.8]` respectively) — the bounds are real guards against genuinely-unbounded raw ratios, not decorative. Every per-channel color computation in the new function is a true convex combination bounded by construction, with the final scale-then-clamp step correctly catching the one place (`scale > 1`) where the pre-clamp product could exceed `1`. The composed pipeline (blackbody → 5.5× saturation cap → arm-blue grade) stays in `[0,1]` by chained construction, not just for the specific constants tested. Build is clean. Ready to merge as the complete color pass.

---

# Validator Audit Report — galaxy_system.h code-health split (dust/nebula builder extraction)

**Methodology:** Behavioral-equivalence review, same bar as the earlier `renderer.h` split audit (`fcfb87e`, "clean, deviations independently re-verified") — line-by-line comparison of moved code against the pre-refactor original, not just trusting the diff summary or the director's own build verification. Saved `origin/main:galaxy_system.h` (1378 lines) to a scratch file and read it in full alongside the new `galaxy_system.h` (892 lines) and `galaxy_media_build.h` (541 lines). Reviewed in an isolated worktree at `/home/eric/Projects/pgu-gsys-audit` (detached HEAD at branch tip).
**Branch reviewed:** `refactor/galaxy-system-decomposition` @ `cf69159`, one commit ("Split dust and nebula builders out of galaxy_system"). `galaxy_system.h`: 892 lines (was 1378) — confirmed under the 1250-line watchdog limit. `galaxy_media_build.h`: 541 lines (new file). Line counts match the director's claim exactly.
**Status:** **Clean.**

## 1. Pure code motion — confirmed line-by-line, not by trusting the diff summary

Read the full pre-refactor dust/nebula section (`DustSample`/`NebulaSample` structs, `CovarianceFromAxes`, `SmoothStep`/`ValueNoise2D`/`Fbm2D`/`WrappedPhase`/`SpiralDustLaneStrength`, `GenerateDustSamples`/`GenerateNebulaSamples`, `BuildDustHierarchyFromSamples`/`BuildNebulaHierarchyFromSamples`, `BuildDustHierarchy`/`BuildNebulaHierarchy`) side-by-side against `galaxy_media_build.h`'s `GalaxyDustBuilder`/`GalaxyNebulaBuilder`:

* **The noise/lane-turbulence stack (`SmoothStep`, `ValueNoise2D`, `Fbm2D`, `WrappedPhase`, `SpiralDustLaneStrength`) is byte-for-byte identical** — every literal constant checked directly (the `0.34f`/`1.20f`/`0.58f`/`1.75f`/`0.80f`/`0.92f`/`2.10f` frequency multipliers, the `91`/`123`/`151`/`271`/`307`/`181`/`211` stream IDs, the `-0.42f`/`0.10f`/`0.28f`/`0.20f`/`0.26f` lane-shape coefficients) — this is exactly the code independently audited for NaN/div0/determinism two audit passes ago (the dust-turbulence audit), and it's unchanged here down to the character.
* **`DustSample`/`NebulaSample` struct definitions**: identical fields, identical order, identical types.
* **`CovarianceFromAxes`**: identical body. This function is only ever called by dust/nebula generation (confirmed via `grep` — zero call sites remain in the star-hierarchy code), so it correctly has **no leftover copy in `galaxy_system.h`** — both new copies (one per builder class) are needed since the builders are now independent static classes with no shared base, and both are byte-identical to the single pre-refactor definition.
* **`GenerateDustSamples`/`GenerateNebulaSamples`**: every literal, every RNG stream index, every acceptance-loop bound (`14`/`18` attempts), every sigma/optical-depth/brightness formula is unchanged. The only difference is the expected class-boundary mechanical rename: `galaxy_.componentCount`/`galaxy_.seed`/`galaxy_.components` (implicit-`this` member access) became `galaxy.componentCount`/`galaxy.seed`/`galaxy.components` (explicit parameter access), and the method itself went from a `const` member function to a `static` function taking `const Galaxy& galaxy` — exactly the "necessary class-boundary parameter passing" the director's ask anticipated, not a behavior change.
* **`BuildDustHierarchyFromSamples`/`BuildNebulaHierarchyFromSamples`**: identical Morton-sort/bottom-up-merge logic, identical `kMergeFanout=4`/`kMortonScale=2097151.0f` constants, identical weighted-average/covariance-accumulation math, identical diagnostic `std::cout` lines. The only difference is `dustHierarchyNodes_`/`dustRootNodeIndex_` (member access) became `hierarchyNodes`/`rootNodeIndex` (reference parameters) — the same class-boundary rename pattern, verified to not touch any of the actual merge arithmetic.
* **The `Build()` wrapper** (bounds computation + empty-input early-out + hierarchy-build call): identical logic to the pre-refactor `BuildDustHierarchy()`/`BuildNebulaHierarchy()` bodies, just restructured from an instance method reading members into a static method taking/writing explicit output parameters.

**One real, flagged, non-behavioral deviation found:** two inline comments were dropped during the move — `// blue optical depth > green > red: extinction/reddening.` (was attached to the dust color literal) and `// Natural-color H-alpha pink-red plus a small OB-star blue lift.` (preceding the nebula `blueLift` line). Grepped both exact strings across the whole new tree: **zero occurrences anywhere** — confirmed lost, not just relocated. This has **zero effect on compiled behavior** (comments don't affect codegen), so it doesn't compromise "bit-identical" in the sense that matters for this audit, but it's a real content difference from "verbatim," so flagging it per the director's own instruction to flag and justify every non-literal change rather than silently pass it. Recommend restoring both comments in a trivial follow-up — not a merge blocker.

## 2. Wiring — confirmed identical data flow, same call order, no dangling/double ownership

`BuildDustHierarchy()`/`BuildNebulaHierarchy()` in the new `galaxy_system.h` are now exactly one line each: `GalaxyDustBuilder::Build(galaxy_, dustHierarchyNodes_, dustRootNodeIndex_);` / `GalaxyNebulaBuilder::Build(galaxy_, nebulaHierarchyNodes_, nebulaRootNodeIndex_);` — passing `GalaxySystem`'s own existing member vectors **by reference** into the static builder, which fills them in place exactly as the old inline method body did directly. `dustHierarchyNodes_`/`dustRootNodeIndex_`/`nebulaHierarchyNodes_`/`nebulaRootNodeIndex_` **remain members of `GalaxySystem`** — not moved, not duplicated — so `SelectDustCut`/`SelectNebulaCut` (the cut-traversal/render path, confirmed untouched in §3) read the exact same state, populated by the exact same values, as before. Verified the call order in `RegenerateGalaxy()` is unchanged: star hierarchy → dust → nebula, then the three `*NodeExpandedLastFrame_.assign(...)` hysteresis resets — identical sequence, identical target members, both before and after this refactor (checked both files at the call site directly). No members left dangling: grepped for every dust/nebula-hierarchy-related member and confirmed each has exactly one owner (`GalaxySystem`) and one writer (the corresponding builder's `Build()`, called from exactly one place). **GPU/CPU buffer sizing is unaffected and correctly untouched**: `kMaxDustGaussians`/`kMaxNebulaGaussians` (the render-cap constants from `gpu_types.h`, governing `dustPositions_`/`nebulaPositions_` CPU arrays and the GPU buffer descriptors) are a **completely separate constant family** from `kDustLeafCount`/`kNebulaLeafCount` (the generation leaf counts, which correctly moved into the builders since they're only used by sample generation) — confirmed no mixing between the two: `kMaxDustGaussians`/`kMaxNebulaGaussians` still appear only in `galaxy_system.h` (the CPU vector resizes and the `SelectDustCut`/`SelectNebulaCut` bounds guards), never in `galaxy_media_build.h`.

## 3. Star hierarchy, coverage floor, colorWeight decouple, EWA projection — confirmed untouched, not just unlikely-to-be-touched

This is the property the whole audit hinges on, so verified it as strongly as possible: `git diff origin/main...HEAD -- galaxy_system.h`, filtered to genuinely-added lines (`grep -E "^\+[^+]" | grep -v "^+++"`), returns **exactly three lines total**: the new `#include "galaxy_media_build.h"` and the two one-line builder-call replacements shown in §2. Every other line in the diff is a **removal** (the extracted dust/nebula code being deleted from this file). This means the star hierarchy build (`BuildHierarchy`), the cut-selection/traversal code (`SelectCut`/`SelectDustCut`/`SelectNebulaCut`/`EmitGaussian`/`EmitDustGaussian`/`EmitNebulaGaussian`), the EWA projection state (`UpdateEwaProjectionState`), and every coverage-floor/telemetry function from two audits ago (`AcesFilmicScalar`, `HdrForDisplayFloor`, `VisibleRadiusPx`, `EquivalentAxisRadiusPx`, `AttributeColor`, `ResetCoverageTelemetry`, `AccumulateCoverageTelemetry`, `FinalizeCoverageTelemetry`, `RecomputeCoverageTelemetryFromCut`, `ApplyCoverageFootprintFloor`, `kTargetCoverageOverlap`, `kMaxCoverageFootprintScale`) and the star-color decouple from three audits ago (`colorWeight`/`colorWeightSum`/`invColorWeight` in the star merge loop) **do not appear anywhere in this diff at all** — not modified, not touched, not even present as context lines that were re-typed. Grepped all of them directly in the new `galaxy_system.h` to confirm continued presence (they're all there, at the same relative structure) — this is un-perturbed-by-construction, the strongest form of "untouched" this audit could establish.

## 4. No ODR/include issues

Both new/changed headers are `#pragma once`-guarded. `galaxy_media_build.h` includes only `<algorithm>`/`<array>`/`<cmath>`/`<cstdint>`/`<iostream>`/`<limits>`/`<utility>`/`<vector>` and `galaxy_model.h` — **no include of `galaxy_system.h`**, so no circular include. `galaxy_system.h` includes `galaxy_media_build.h` plus its own independent (slightly redundant but harmless, and correct — an explicit declaration of its own direct dependency) `#include "galaxy_model.h"`. Checked every other include site of these three headers project-wide (`main.cpp`, `tuning_panel.h`, `renderer.h`, `tests/aspect_ratio_clamp_test.cpp`) — all go through the same `#pragma once`-guarded pattern already established elsewhere in this codebase (e.g. the `renderer_postprocess.h`/`renderer_screenshot.h` split from the earlier renderer audit). No ODR risk.

## 5. Fresh build — confirmed clean, independently

Ran my own `cmake -S . -B build` + `cmake --build build -j32` in the isolated worktree (not trusting the director's own reported build): **exit 0**, no warnings, all targets linked successfully.

## 6. `tests/aspect_ratio_clamp_test.cpp` — confirmed pre-existing breakage, not newly broken by this refactor

`git diff origin/main...HEAD -- tests/aspect_ratio_clamp_test.cpp` returns **empty** — the test file itself is byte-identical between `origin/main` and this branch, untouched by this refactor. Compiled it directly (`g++ -std=c++20 -O0 -I. -c tests/aspect_ratio_clamp_test.cpp`) to see the actual error rather than trusting the director's characterization: `no matching function for call to 'GalaxySystem::BuildCut(...)' ... candidate expects 6 arguments, 5 provided` — this is the stale 5-argument call from before the `exposure` parameter was added to `BuildCut` (the coverage-overlap audit, several passes back) — confirmed pre-existing and unrelated to this refactor's dust/nebula extraction. Checked the other three test files (`drag_resize_corruption_test.py`, `resize_present_shear_test.sh`, `viewport_bleed_test.py`) for any dependency on `galaxy_system.h`/`galaxy_media_build.h`/the builder classes — none found, nothing else at risk.

## 7. Verdict

**Clean.** This is genuine, verified code motion, not a rewrite: every function moved into `GalaxyDustBuilder`/`GalaxyNebulaBuilder` was compared line-by-line against the pre-refactor original (not sampled, not spot-checked) and found identical modulo the expected member-to-parameter class-boundary rename — including the previously-independently-audited noise/turbulence stack, which is unchanged down to every literal constant. `galaxy_system.h`'s diff is provably minimal: exactly 3 added lines (1 include, 2 one-line builder calls), meaning the star hierarchy, cut-selection/emission, EWA projection, coverage footprint floor, and colorWeight decouple are not merely "probably fine" but literally absent from the diff — un-perturbed by construction. Wiring is correct: same member vectors, same call order, same buffer-sizing constants, no dangling or double ownership. No ODR/circular-include issues. Build verified clean independently. The one pre-existing test breakage was independently reproduced and confirmed unrelated to this refactor (stale argument count from an earlier, unrelated branch). One real but purely cosmetic deviation found and flagged: two inline comments were dropped during the move (zero behavioral effect, worth a trivial follow-up restore). Recommend merging to `main`.

---

# Validator Audit Report — Stage 1: resolved-bright point-star depth layer

**Methodology:** Correctness audit of a genuinely new feature (not a refactor) — the first stage of `docs/galaxy_layered_render_architecture.md`'s layered-render plan. Read the full design doc before touching code, since the "must-not-regress" bar and the "why depth-write matters" rationale are only legible against it. Reviewed in an isolated worktree at `/home/eric/Projects/pgu-stage1-audit` (detached HEAD at branch tip).
**Branch reviewed:** `feature/stage1-resolved-stars` @ `27cb101`, 2 commits: `eaca8a5` ("Split resolved bright stars into depth layer" — the full feature) and `27cb101` ("Lower resolved-star split threshold" — one-constant change, `600.0f→20.0f` L☉). Diff: `CMakeLists.txt`/`cmake/embed_shaders.cmake` (new shader plumbing), `galaxy_model.h` (+1 field on `StarSample`), `galaxy_system.h` (+86, the classification/emission logic), `gpu_types.h` (+1 constant), `main.cpp` (+wiring), `renderer.h` (+106, the new pipeline/buffers/draw call), `shaders/resolved_star.wgsl` (new file).
**Status:** **Clean**, with one spec-ambiguity finding in the new shader worth a zero-risk preventive fix (not a confirmed bug), and the usual "can't visually confirm" caveat for the numeric field-mean claim.

## 1. MUST-NOT-REGRESS — traced to the generation-loop level, not just "looks unchanged"

The classification loop in `RegenerateGalaxy()` is the only place the smooth-field input changes:
```
for i in [0, leafCount):
    star = GenerateStar(galaxy_, i)
    if IsResolvedBrightStar(star): classify into resolvedStars_, continue
    else: fold into boundsMin/boundsMax, push into stars[]
BuildHierarchy(stars, boundsMin, boundsMax)   // unchanged function, unchanged math
BuildDustHierarchy()  // GalaxyDustBuilder::Build(galaxy_, ...) — regenerates from galaxy_ directly, never reads `stars`
BuildNebulaHierarchy() // same — GalaxyNebulaBuilder::Build(galaxy_, ...), independent of `stars`
```
**The key correctness property, verified rather than assumed:** `GenerateStar(galaxy_, i)` is a pure, deterministic function of `(galaxy_, i)` (hash-based RNG, no shared/incidental state — established in earlier audits and unchanged by this branch). Because of that purity, filtering the loop's output by `IsResolvedBrightStar` cannot alter what any *surviving* star's sampled attributes are — each unresolved star `i` gets exactly the same `StarSample` value it would have gotten before this branch existed, whether or not other indices got filtered out. So the set fed into `BuildHierarchy` is provably `{GenerateStar(galaxy_,i) : i, not resolved}`, each entry bit-identical to its pre-branch value — this is the literal meaning of "only subtracted," not just a description of intent.

**Dust and nebula are unaffected by construction, not just by testing:** `BuildDustHierarchy()`/`BuildNebulaHierarchy()` call `GalaxyDustBuilder::Build(galaxy_, ...)`/`GalaxyNebulaBuilder::Build(galaxy_, ...)`, which (per the prior code-motion audit) regenerate their own sample sets directly from `galaxy_` — they never read the `stars` vector at all. Since this branch's diff touches zero lines in `galaxy_media_build.h` (confirmed via `git diff --stat`), dust and nebula generation cannot be affected by anything this branch does, structurally, independent of what the star classification does.

**The `boundsMin`/`boundsMax` question, addressed directly rather than glossed over:** the bounding box fed to `BuildHierarchy`'s Morton-quantization is now computed only over unresolved stars — since resolved-bright stars (by design, often the isolated sparse-region outliers) are excluded from the min/max fold, the box can be measurably smaller than before. This *is* a numeric difference beyond "fewer stars in the list" — but it's not a *second, independent* change: the bounding box is a **derived quantity of the star set fed into `BuildHierarchy`**, not an independent input, so its narrowing is fully entailed by the one actual change (which stars are in that set) rather than evidence of something else being altered. The bounding box affects only the *Morton sort order* used to decide which adjacent leaves get merged into which parent (the actual per-star position/covariance/brightness values used in the merge math are always full-precision, never quantized) — and because the bottom-up merge is strictly energy-conserving regardless of pairing (`brightnessSum` is a plain additive sum at every level, established in three earlier audits), the field's *total* emitted energy is unaffected by which unresolved stars happen to pair with which under a shifted quantization. The genuinely-open, `renderer`-side question this can't settle statically is whether a different Morton pairing shifts the merged-node *shapes* enough to be visible in the *texture* of the smooth field at a given LOD — a real but second-order, energy-neutral effect that's an unavoidable consequence of the one intended change (fewer stars → smaller box), not evidence of an unrelated alteration.

**On the specific `0.2048→0.2004` field-mean number:** the mechanism is directionally and structurally consistent with the claim — pulling out ~442 stars whose real luminosity is ≥20 L☉ (genuinely bright relative to the population) necessarily removes their (compression-bounded, since `renderLuminosity` caps at `16×2.65=42.4` regardless of how luminous the star really is) contribution from the additive field sum, so a small downward shift in mean is the expected direction and a ~2% relative change is a plausible magnitude for ~442 stars out of a much larger population, each individually capped rather than able to dominate the sum. **Can't independently verify the exact figures** — no GPU/display in this container to render and measure a real frame; this is a structural/directional check, not a pixel-measured one, same caveat as every prior fidelity audit in this series.

## 2. THE NEW POINT-STAR LAYER — crisp, never-floored, genuinely depth-writing (traced to the literal object, not the comment)

**Crisp points, not gaussian-splatted haze — confirmed from the actual vertex math, not the file being new.** `resolved_star.wgsl`'s `vs_main` offsets the quad corners by `input.radius_px * ndc_per_px` — a **fixed screen-space pixel radius** (`ndc_per_px = 2/viewport_size`, a pure NDC-per-pixel conversion), independent of the star's distance from camera or any EWA-projected covariance axis. This is structurally different from the field splats (`splat.wgsl`, which scale by `axis1`/`axis2` — distance-correct, EWA-projected ellipse half-extents). `attr.axis1`/`attr.axis2` are explicitly zeroed in `EmitResolvedStar` and, confirmed by checking the pipeline's vertex-buffer-layout attribute list, **never read** by this pipeline at all (only offsets 16/20/32 — `amplitude`-as-`radius_px`, `color`, `brightness` — are bound as attributes) — so there's no leftover EWA-shape influence on this layer whatsoever.

**Never coverage-floored — confirmed by call-order and by the arrays never overlapping, not just by the stars being generated separately.** `ApplyCoverageFootprintFloor()` (unchanged, traced in an earlier audit) operates exclusively on `pointStaticAttributes_`/`cutGaussianCount_`. `EmitResolvedStar` writes into a **completely separate pair of member vectors** (`resolvedStarPositions_`/`resolvedStarStaticAttributes_`), populated in a loop that runs *after* `ApplyCoverageFootprintFloor()`/`RecomputeCoverageTelemetryFromCut()` in `BuildCut()`'s call order. Two independent guarantees stack here: (1) resolved stars never enter `hierarchyNodes_` at all (filtered out before `BuildHierarchy` even runs, confirmed in §1), so they can't be floored *indirectly* via node merging either; (2) even if they somehow had ended up in the cut arrays, the floor function's own array/index space (`pointStaticAttributes_`) is disjoint from theirs. Both "bypasses merge" and "bypasses the coverage floor" from the design doc are independently true, not just one implying the other.

**Genuinely writes depth — traced the actual `wgpu::DepthStencilState` object identity, the specific thing the director asked to verify isn't "just claimed."** `resolvedStarPipeline_`'s `pipelineDescriptor.depthStencil = &hardwareDepthStencil;` (renderer.h) — `hardwareDepthStencil` is the **same C++ local variable**, not a new/differently-configured one, already used by the pre-existing `hardwarePointPipeline_` (confirmed via `grep`: exactly one definition at renderer.h:202-205, two use sites at :270 and :432, no shadowing). That definition sets `depthWriteEnabled = true` explicitly. This pipeline draws inside the same render pass as the star/splat draws (same `pass` object, before `pass.End()`), which has a real depth attachment bound (`depthView_`, `Depth24Plus`, matching this pipeline's declared depth format — consistent attachment, per the methodology from the very first audit in this series). So resolved-star fragments that pass the depth test **genuinely write into `depthView_`**, which persists for the rest of the frame — this is not inferred from a comment, it's the same object reference already proven to have `depthWriteEnabled=true` in an untouched, pre-existing code path.

**Per-star blackbody color preserved:** `EmitResolvedStar` writes `attr.color[i] = star.color.{x,y,z}` directly from `StarSample.color` — the same field already produced by the full, previously-audited `BlackbodyToRGB → SaturateHotStarColor → GradeYoungArmColor` pipeline (unchanged by this branch — `galaxy_model.h`'s diff only adds the new `luminosity` field, doesn't touch the color pipeline at all). No separate/simplified color path for this layer.

## 3. Classification, NaN/div0, double-counting, buffer sizing

* **`IsResolvedBrightStar`**: `star.luminosity >= kResolvedStarLuminosityThreshold` — a plain threshold comparison on `luminosity`, the **raw sampled luminosity local variable** from `ApplyStellarPopulationColor` (confirmed by reading the function: `luminosity = lerp(Lmin, Lmax, f)`, the same real-luminosity value already used for `colorWeight` in the color-decouple audit — not the compressed `renderLuminosity`). No division, no log/sqrt in the classification itself — no NaN/div0 possible.
* **No double-counting**: the `if (IsResolvedBrightStar(star)) { ...; continue; }` structure is a mutually-exclusive branch — a star is pushed into exactly one of `resolvedStars_` or `stars`, never both, by construction of the `continue`.
* **The 20 L☉ threshold is astronomically sane**, cross-checked against the catalog from the earlier stellar-population audit: it sits inside the A-dwarf range (5–25 L☉, catching the upper half), and captures *all* B dwarfs (25–30,000), *all* O dwarfs (30,000–1,000,000), and the K/M giant branch (100–3,000) — a reasonable "resolved bright star" cut, while leaving white dwarfs (0.0001–0.01 L☉, despite being hot) correctly in the unresolved field.
* **Buffer sizing, checked at both the classification cap and the GPU allocation**: `kMaxResolvedStars = 8192` (`gpu_types.h`). The generation-time cap (`if (resolvedStars_.size() < kMaxResolvedStars) resolvedStars_.push_back(star);`) is the actual enforcement point — `resolvedStarPopulationCount_` still counts every qualifying star *before* the cap (true-population telemetry), while `resolvedStars_` itself is hard-capped. `EmitResolvedStar` has a redundant-but-correct second guard (`if (resolvedStarCount_ >= kMaxResolvedStars) return;`) that can never actually trigger given the first cap, but is harmless. CPU vectors (`resolvedStarPositions_`/`resolvedStarStaticAttributes_`, sized to `kMaxResolvedStars` in the constructor) and the two new GPU buffer descriptors (`resolvedPositionBufferDescriptor.size`/`resolvedStaticBufferDescriptor.size`, both `kMaxResolvedStars * sizeof(...)`) all key off the same constant — no overflow possible anywhere in the new layer. At the reported ~442 stars for the 20 L☉ threshold, this cap has enormous headroom; noting (not a bug, a capacity observation) that if `generatedStarCount`/arm-density tuning were pushed dramatically higher in the future, excess resolved stars beyond the cap are silently dropped rather than crashing — the same graceful-truncation pattern already accepted for `kMaxCutGaussians`/`kMaxDustGaussians`/`kMaxNebulaGaussians` in prior audits.
* **`brightnessT`/`sqrt` safety**: `maxCompressedBrightness = 2.65×16.0 = 42.4` is exactly `star.brightness`'s true mathematical ceiling (`renderLuminosity` caps at `16.0`, `×kLuminosityBrightnessScale=2.65`), so `brightnessT = star.brightness/42.4` is bounded `[0,1]` **by construction**, not just by the explicit (redundant-but-harmless) `clamp` — `sqrt(brightnessT)` is therefore always applied to a non-negative operand, no NaN possible.
* **`RenderFrame`'s new parameter**, inserted mid-signature (`..., cutGaussianCount, resolvedStarCount, dustGaussianCount, nebulaGaussianCount`) — checked the call site in `main.cpp` positionally against the new signature (all four are `uint32_t`, so a swap wouldn't be a compile error, meaning this needed manual verification, not just "it built"): `CutGaussianCount()`, `ResolvedStarCount()`, `DustGaussianCount()`, `NebulaGaussianCount()` — matches the parameter order exactly, confirmed by reading both sides, not inferred from successful compilation.

**One finding worth flagging — a spec-ambiguity in the new shader, not a confirmed bug.** `resolved_star.wgsl`'s `fs_main`: `let core = smoothstep(1.0, 0.45, sqrt(r2));` — the edges are given in **descending** order (`1.0 > 0.45`), unlike every other `smoothstep`/`SmoothStep` call anywhere else in this codebase (`dust_extinction.wgsl`, `postprocess.wgsl`, `galaxy_model.h`, `galaxy_media_build.h` — all consistently ascending). Hand-derived what this produces under the literal `t=clamp((x-low)/(high-low),0,1); return t²(3-2t)` formula: with `low=1.0, high=0.45`, the ramp direction flips (denominator negative), giving `t=1.0` (flat, full brightness) for `x∈[0,0.45]` and a smooth fall to `t=0` at `x=1.0` — a sensible, apparently-intentional "flat bright disk core, soft edge to the sprite boundary" shape, consistent with wanting a crisp-but-not-hard-edged point. **What I could not verify in this container** (no GPU/display, and I want to be honest about the limit of static review here): whether the WGSL specification treats `low > high` as *fully defined* (some shading-language smoothstep specs, e.g. classic GLSL, explicitly say "results are undefined if edge0 ≥ edge1"; if WGSL's normative text carries the same caveat rather than mandating the literal clamp-based formula unconditionally, this expression's behavior could in principle differ across WGSL backends/compiler versions even though it works out sensibly under the straightforward formula). Given the fix is free — `1.0 - smoothstep(0.45, 1.0, sqrt(r2))` is mathematically identical under the clamp formula and uses unambiguous ascending edges — recommend making this swap as zero-risk insurance rather than relying on an edge case that isn't otherwise used anywhere in this codebase, even though I can't point to a concrete failure.

## 4. Build — clean, shader embedding confirmed

Fresh `cmake -S . -B build` + `cmake --build build -j32`: **exit 0**, no warnings, all targets linked. Checked the actual generated shader header (`build/generated/shaders_generated.h`) rather than just trusting the build succeeded: `kResolvedStarShader` is present, embedded between `kSplatShader` and `kDustExtinctionShader` — confirmed the new shader file is genuinely wired into the embed step (`CMakeLists.txt`'s `SHADER_SOURCES` list and `embed_shaders.cmake`'s new `RESOLVED_STAR_WGSL` input both correctly updated). No test file references `StarSample`/`resolvedStar*`/`kMaxResolvedStars` — nothing new at risk of a stale test assumption; `tests/aspect_ratio_clamp_test.cpp` remains the same pre-existing, unrelated breakage confirmed in the prior audit.

## 5. Verdict

**Clean.** Must-not-regress holds at the strongest level this audit could establish: the field-generation loop's purity means unresolved stars are provably bit-identical to their pre-branch values, dust/nebula are structurally incapable of being affected (they never read the star list), and the one genuine second-order effect (bounding-box narrowing shifting Morton pairing) is fully entailed by the one intended change rather than an independent alteration, with the field's total energy provably unaffected by pairing regardless. The new point-star layer is crisp by actual vertex math (fixed screen-space pixel radius, not EWA-projected), never coverage-floored on two independent grounds (never enters the hierarchy, and writes to disjoint arrays even if it had), and genuinely writes depth — traced to the literal shared `DepthStencilState` object already proven `depthWriteEnabled=true` in an untouched pre-existing pipeline, not inferred from a comment or the field name. Classification is threshold-only (no NaN/div0), provably mutually-exclusive (no double-counting), astronomically sane at 20 L☉, and buffer-sized consistently across CPU/GPU with graceful (non-crashing) overflow handling. Build is clean, shader embedding independently verified in the generated header. One shader-level finding flagged: a reversed-edge `smoothstep` call that produces a sensible result under the straightforward formula but isn't confirmed spec-guaranteed for `low>high` — free, zero-risk fix recommended, not a blocker. Field-mean numeric claim is structurally/directionally consistent but not independently pixel-verified (no display in this container, same standing caveat as every prior fidelity audit here). Recommend merging — Stage 1 complete.

---

# Validator Audit Report — HII densification + knotty-clumping (tune/hii-density)

**Methodology:** Correctness + scope audit of two commits (`453a867`, `dc540b5`) that are the actual deliverable on a branch that also happens to carry unrelated already-on-main commits (a Stage-3 design doc, a `directorctl` typing-detection fix) — per the director's explicit note, reviewed *only* the two named HII commits, not the branch as a whole. Read the current, post-both-commits state of `GenerateNebulaSamples` in full (not just the two diffs in isolation) to trace the combined control flow end-to-end. Reviewed in an isolated worktree at `/home/eric/Projects/pgu-hii-audit` (detached HEAD at branch tip).
**Branch reviewed:** `tune/hii-density` @ `dc540b5`. The two HII commits touch, combined: `galaxy_media_build.h` (the nebula generation logic — the bulk of the change), `galaxy_system.h` (one line, `kNebulaPixelErrorThreshold`), `gpu_types.h` (one line, `kMaxNebulaGaussians`). Confirmed via `git show --stat` on each commit individually (not a combined range, which would have pulled in the stray intervening commits).
**Status:** **Clean.**

## 1. Scope — confirmed via diff content, not just file names

`git show 453a867 --stat` / `git show dc540b5 --stat`: exactly the three files listed above, nothing else. Went further and grepped every added/removed line across both commits for star-field, resolved-star-layer, and dust-path identifiers (`dust`, `resolvedStar`, `StarSample`, `kMaxResolvedStars`, `kMaxDustGaussians`, `kMaxCutGaussians`, `EmitGaussian`, `ApplyCoverageFootprintFloor`, `hierarchyNodes_`) — **zero hits**. The smooth star field, the Stage-1 resolved point-star layer, and the dust extinction path are untouched by either commit, confirmed by content inspection rather than inferred from the file list.

## 2. New clustering/placement logic — traced end-to-end across both commits combined

Read the final, current `GenerateNebulaSamples` (post-both-commits) rather than each diff in isolation, since the two commits restructure the same function sequentially and a diff-only read risks missing how the pieces compose. Traced the full per-leaf flow for `i` in `[0, kNebulaLeafCount=12000)`:

1. `clusterIndex = i/7u`, `memberIndex = i%7u` — clusters of up to 7 potential members, sharing cluster-level randomness (`clusterIndex`-keyed `random01` calls) for host-component pick, radius, and the peak-arm-density angle search; member-level randomness (`i`-keyed) for local scatter, size, and color/brightness variety.
2. **Radius**: `SampleSersicRadius` with a bounded retry (≤5 attempts, escaping early once `r ≤ 1.42·scaleRadius`) followed by an unconditional `std::min(r, 1.48·scaleRadius)` — bounded regardless of how the retries resolve, no unbounded growth, no infinite loop (fixed attempt count).
3. **Angle**: a 48-candidate *maximization* (not rejection sampling) — take the proposal with the highest `armFactor/maxArmFactor` among 48 draws. `maxArmFactor = 1+clamp(armContrast,0,0.98) ∈ [1,1.98]`, so this division's denominator is bounded strictly away from zero; the resulting `normalizedArm` is the max of 48 already-`clamp`-ed `[0,1]` values, itself in `[0,1]`. This is a genuine, intentional distributional shift from the old rejection-sampling approach (biases much harder toward the arm-density peak) — consistent with the commit's own name ("Densify arm-hugging"), an aesthetic/tuning choice the director has already signed off on, not a correctness concern.
4. **Active-cluster gate**: `if (random01(seed, clusterIndex, 71) > 0.46+0.24·normalizedArm) continue;` — keyed by `clusterIndex`, so this decision is identical across all 7 members of a cluster (re-evaluated redundantly per member rather than hoisted out of the loop, a minor efficiency note, not a correctness issue — the outcome is consistent regardless of how many times the same deterministic expression is evaluated). This is the "active-cluster probability" the director's ask named.
5. **Member count**: `memberCount = 1 + floor(random01(seed,clusterIndex,72)·7.0)`. Since `random01∈[0,1)` strictly, `random01·7.0∈[0,7)` strictly, `floor(...)∈{0,...,6}`, so `memberCount∈{1,...,7}` — **exactly** the "1–7 members" the director's ask named, with no off-by-one risk at either end (verified the strict `<1` bound on `random01` is what prevents `floor` from ever reaching `7`). `if (memberIndex >= memberCount) continue;` correctly gates only the first `memberCount` of the 7 potential slots.
6. **Jitter + the halo gate**: `angle += clusterJitter + memberJitter` (both bounded, straightforward), then `normalizedArm` is recomputed post-jitter (same safe division pattern), and **`if (normalizedArm < 0.54f) continue;`** — this is the literal "normalizedArm>=0.54 halo gate" from the director's ask, confirmed present and exactly matching the stated threshold. This is a hard, deterministic (not probabilistic) floor — even after jittering a cluster's angle away from its peak-density position, a member must still land in a region at or above 54% of the local peak arm density to survive, which is what prevents scatter into the low-density inter-arm/halo — directly implementing the layered-render design doc's stated L4 fix ("gate HII placement hard to arm density... stop scattering into the halo").
7. **Position**: `radialOffset` and `localR = clamp(r+radialOffset, 0.06·scaleRadius, 1.52·scaleRadius)` — explicitly clamped to a bounded, strictly-positive-floor range (assuming `comp.scaleRadius>0`, the same pre-existing unchanged assumption every other generator in this codebase already relies on). No negative-radius risk.
8. **Size**: `complexScale ∈ {0.78, 1.45, 2.25}` (a clean 3-way discrete tier via nested ternary on `complexDraw`, cluster-level so shared across a complex's members — this is the "complexScale... size variation" named in the ask) and `memberScale = 0.62+0.78·random01 ∈ [0.62,1.40)` (per-member, the "memberScale" counterpart). `coreSigma = (0.0032+0.010·sizeDraw²)·complexScale·memberScale·max(0.8,scaleRadius/3.2)` — a product of four strictly-positive factors (verified each individually: `0.0032>0` floor on the first term, `complexScale` and `memberScale` both strictly positive by their ranges, the `max(0.8,...)` floor on the last), so `coreSigma` is always strictly positive — never zero/negative, which matters because a zero/negative sigma would degenerate the covariance. `verticalSigma` and the second-axis sigma multiplier (`0.62+0.48·random01`) are both similarly floored-positive.
9. **Covariance/effRadius**: same pre-existing `CovarianceFromAxes`/`3·sqrt(max(0,·))` pattern, unchanged, fed only strictly-positive sigmas — produces a valid PD covariance every time, confirmed by construction rather than assumed.
10. **Color/brightness**: `color` formula unchanged in shape from the prior nebula-pop audit (still `ClampColor`-wrapped, still bounded); `brightness` is a product of three strictly-positive, individually-bounded factors (verified each: `[0.46,1.56)`, `[0.8716,1.12]` given `normalizedArm∈[0.54,1]` is now guaranteed by the gate at this point in the code, `{0.954,1.155,1.395}` for the three `complexScale` tiers) — positive and bounded, no NaN/overflow risk.

**No NaN/Inf/div0 anywhere**: the only division in the new code (`armFactor/maxArmFactor`) has a denominator provably bounded in `[1,1.98]`; every `sqrt` argument is floored at `0` first; every sigma feeding the covariance is provably strictly positive. **Deterministic**: every new random draw is `random01(seed, clusterIndex_or_i, stream)` — both indices are pure functions of the deterministic loop counter `i`, and `random01` itself is the codebase's already-established pure hash function (no incidental/hidden state) — same seed reproduces identical clusters, member counts, jitter, and gating decisions. **Positions/covariances stay valid**: traced explicitly above, not assumed from "it compiled."

## 3. Buffer sizing — same proven-safe pattern, re-verified at the new numbers

`kMaxNebulaGaussians` (the render-cap constant, `gpu_types.h`): `4096→8192` (commit 1 only; commit 2 doesn't touch `gpu_types.h`, confirmed via its diff). Checked every consumer: CPU vectors `nebulaPositions_`/`nebulaStaticAttributes_` (resized to `kMaxNebulaGaussians` in the `GalaxySystem` constructor, unchanged code — the resize call itself wasn't touched, it just now reads the updated constant), the `SelectNebulaCut` bounds guard (`if (nebulaGaussianCount_ >= kMaxNebulaGaussians) return;`, unchanged code), and the two GPU buffer descriptors in `renderer.h` (`nebulaPositionBufferDescriptor.size`/`nebulaStaticBufferDescriptor.size`, both `kMaxNebulaGaussians * sizeof(...)`, unchanged code — `renderer.h` isn't touched by either HII commit at all). All four consumers key off the single constant; no hardcoded old value anywhere (this is now the fourth time across this audit series this exact pattern — cap bump, single source of truth, all consumers scale automatically — has been verified clean).

`kNebulaLeafCount` (the generation count, `galaxy_media_build.h`): `3200→9000→12000` across the two commits. `12000 > kMaxNebulaGaussians=8192` — the same `leafCount > cap` situation independently verified safe three times already in this audit series (dust and stars both run this exact pattern): `BuildHierarchyFromSamples` for nebula (unchanged by either commit — confirmed via `grep`, no diff hunk touches it) builds its full Morton-merge tree off `nebulae.size()` with zero reference to `kMaxNebulaGaussians`; the cap only gates the top-down `SelectNebulaCut` traversal at the opposite end of the pipeline. Note `nebulae.size()` will be somewhat less than the full `12000` in practice — the active-cluster gate, member-count gate, and the `normalizedArm<0.54` halo gate all `continue` past unqualified candidates — but this doesn't change the safety argument: whatever the actual generated count turns out to be, the hierarchy-build/cut-selection split handles it identically regardless of magnitude, and the pre-existing empty-`nebulae` guard in `Build()` (unchanged) handles the degenerate all-filtered-out case safely too.

## 4. Build — clean

Fresh `cmake -S . -B build` + `cmake --build build -j32` (on the branch as checked out, including the stray unrelated commits the director said to ignore for *review* purposes — building the actual checkout is still the correct thing to verify): **exit 0**, no warnings, all targets linked. No test file references `kNebulaLeafCount`/`kMaxNebulaGaussians`/`GenerateNebulaSamples` — nothing at risk of a stale assumption.

## 5. Verdict

**Clean.** Scope is confirmed by diff content, not just file names: zero lines in either commit touch the star field, the Stage-1 resolved-star layer, or the dust path. The new clustering logic — cluster-shared host/radius/peak-angle sampling, the active-cluster probability gate, the 1–7 member-count draw, the post-jitter `normalizedArm≥0.54` halo gate, and the `complexScale`/`memberScale` size-variation pair — was traced end-to-end through the combined post-both-commits code, not just diffed in isolation, and every piece named in the director's ask was located and confirmed to match exactly (including the specific "1-7" range and the specific "0.54" threshold). No NaN/Inf/div0 anywhere: the one division has a provably-bounded-away-from-zero denominator, every sigma feeding the covariance is provably strictly positive, every `sqrt` argument is floored. Fully deterministic (same pure-`random01`-chain pattern as everything else in this codebase). Buffer sizing follows the same now-four-times-verified safe pattern: the cap (`kMaxNebulaGaussians=8192`) and the leaf count (`kNebulaLeafCount=12000`) are two independent constant families with no cross-contamination, and the `leafCount>cap` case is safe because hierarchy construction has zero dependency on the render cap. Build is clean. Recommend merging (cherry-pick the two HII commits onto `main`, per the director's stated plan).
